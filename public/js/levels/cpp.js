import { vHas } from './helpers.js';

export const CPP_LEVELS = [
  { id:9001, title:'Hello World', category:'C++', type:'cpp',
    question:'Print "Hello, World!" using cout.',
    hints:['cout uses << to output','Text in double quotes','cout << "Hello, World!" << endl;'],
    validate: vHas(['cout','Hello, World']),
    successMessage:'cout sends text to stdout. endl flushes the buffer and adds a newline; "\\n" is faster when flushing isn\'t needed.' },

  { id:9002, title:'Include iostream', category:'C++', type:'cpp',
    question:'Write the #include directive for cout and cin.',
    hints:['#include brings in headers','iostream = input/output streams','#include <iostream>'],
    validate: vHas(['#include','<iostream>']),
    successMessage:'<iostream> gives you cout, cin, cerr, clog. Angle brackets for standard headers, quotes for your own.' },

  { id:9003, title:'Integer Variable', category:'C++', type:'cpp',
    question:'Declare an int "score" set to 100.',
    hints:['int for whole numbers','int name = value;','int score = 100;'],
    validate: vHas(['int','score','100']),
    successMessage:'int is typically 32 bits. C++ also has short, long, long long, and unsigned variants.' },

  { id:9004, title:'Double Variable', category:'C++', type:'cpp',
    question:'Declare a double "price" set to 9.99.',
    hints:['double for decimal numbers','double name = value;','double price = 9.99;'],
    validate: vHas(['double','price','9.99']),
    successMessage:'double is 64-bit floating point. float is 32-bit (suffix f: 9.99f). double is preferred.' },

  { id:9005, title:'String Variable', category:'C++', type:'cpp',
    question:'Declare a std::string "name" with value "Alex".',
    hints:['Include <string>','string name = "value";','std::string name = "Alex";'],
    validate: vHas(['string','name','Alex']),
    successMessage:'std::string is dynamic and safe. C-style char arrays exist but std::string is almost always better.' },

  { id:9006, title:'Boolean Variable', category:'C++', type:'cpp',
    question:'Declare a bool "isActive" set to true.',
    hints:['bool holds true or false','lowercase true/false','bool isActive = true;'],
    validate: vHas(['bool','isActive','true']),
    successMessage:'bool stores true (1) or false (0). Any non-zero value is truthy in conditions.' },

  { id:9007, title:'Console Input', category:'C++', type:'cpp',
    question:'Use cin to read an integer into a variable "age".',
    hints:['cin uses >> to extract','Declare variable first','int age; cin >> age;'],
    validate: vHas(['cin','>>','age']),
    successMessage:'cin >> skips whitespace. Use getline(cin, str) to read full lines including spaces.' },

  { id:9008, title:'If Statement', category:'C++', type:'cpp',
    question:'Write an if that checks score > 50 and prints "Pass".',
    hints:['if (condition) { body }','Use cout to print','if (score > 50) { cout << "Pass"; }'],
    validate: vHas(['if','score','cout']),
    successMessage:'Parentheses around the condition are required. Braces define the block.' },

  { id:9009, title:'If-Else', category:'C++', type:'cpp',
    question:'Print "Adult" if age >= 18, "Minor" otherwise.',
    hints:['else follows if\'s closing brace','Both need cout','if (age >= 18) { cout << "Adult"; } else { cout << "Minor"; }'],
    validate: vHas(['if','else','cout']),
    successMessage:'Chain with else if for multiple conditions. For discrete values, consider switch.' },

  { id:9010, title:'Switch Statement', category:'C++', type:'cpp',
    question:'Write a switch on "choice" with cases 1 and 2, plus default.',
    hints:['switch(var) { case: ... break; }','break prevents fall-through','switch (choice) { case 1: break; case 2: break; default: break; }'],
    validate: vHas(['switch','case','break','default']),
    successMessage:'Without break, execution falls through. Sometimes intentional, usually a bug.' },

  { id:9011, title:'For Loop', category:'C++', type:'cpp',
    question:'Write a for loop printing 1 through 5.',
    hints:['for (init; cond; update)','i++ increments','for (int i = 1; i <= 5; i++) { cout << i << endl; }'],
    validate: vHas(['for','int i','cout']),
    successMessage:'Three parts: init (once), condition (each iter), update (after each iter).' },

  { id:9012, title:'While Loop', category:'C++', type:'cpp',
    question:'Write a while loop counting 1 to 3.',
    hints:['while (condition) { body }','Increment your counter','int i = 1; while (i <= 3) { cout << i; i++; }'],
    validate: vHas(['while','cout']),
    successMessage:'while repeats while condition is true. Ensure the condition eventually becomes false.' },

  { id:9013, title:'Array', category:'C++', type:'cpp',
    question:'Declare an int array "nums" with values 10, 20, 30.',
    hints:['type name[] = {values}','Use curly braces','int nums[] = {10, 20, 30};'],
    validate: vHas(['int','nums','10','20','30']),
    successMessage:'C-style arrays are fixed-size. Prefer std::array or std::vector in modern C++.' },

  { id:9014, title:'Vector', category:'C++', type:'cpp',
    question:'Create a vector<int> "nums" with values 1, 2, 3.',
    hints:['#include <vector>','vector<type> name = {vals}','std::vector<int> nums = {1, 2, 3};'],
    validate: vHas(['vector<int>','nums','1','2','3']),
    successMessage:'std::vector is a dynamic array. Most commonly used container — your default choice.' },

  { id:9015, title:'Vector push_back', category:'C++', type:'cpp',
    question:'Add value 42 to a vector "nums" using push_back.',
    hints:['push_back appends to end','Called on the vector','nums.push_back(42);'],
    validate: vHas(['.push_back(','42']),
    successMessage:'push_back appends. pop_back removes last. emplace_back constructs in place (more efficient).' },

  { id:9016, title:'Range-Based For', category:'C++', type:'cpp',
    question:'Use a range-based for to print every element in vector "nums".',
    hints:['for (type item : container)','auto deduces type','for (auto n : nums) { cout << n; }'],
    validate: vHas(['for',':','cout']),
    successMessage:'Range-based for (C++11) is cleaner than index loops. Use const auto& to avoid copies.' },

  { id:9017, title:'Function', category:'C++', type:'cpp',
    question:'Write a function "add" taking two ints, returning their sum.',
    hints:['Return type before name','return sends value back','int add(int a, int b) { return a + b; }'],
    validate: vHas(['int add','return','a + b']),
    successMessage:'Functions must be declared before use. Return type, name, params form the signature.' },

  { id:9018, title:'Function Overloading', category:'C++', type:'cpp',
    question:'Write two "add" functions: one for ints, one for doubles.',
    hints:['Same name, different params','Compiler picks the right one','int add(int a, int b) { return a + b; } double add(double a, double b) { return a + b; }'],
    validate: vHas(['int add(int','double add(double']),
    successMessage:'Overloading reuses a name for different types. Resolved at compile time.' },

  { id:9019, title:'Pass by Reference', category:'C++', type:'cpp',
    question:'Write a function "increment" taking an int by reference, increasing it by 1.',
    hints:['& after type = reference','Original variable is modified','void increment(int& x) { x++; }'],
    validate: vHas(['int&','increment']),
    successMessage:'Pass by reference avoids copies and lets the function modify the original. Use const& for read-only.' },

  { id:9020, title:'Pointer', category:'C++', type:'cpp',
    question:'Declare int "x" = 10, then pointer "ptr" pointing to x.',
    hints:['* declares a pointer','& gets address','int x = 10; int* ptr = &x;'],
    validate: vHas(['int','x','10','ptr','&x']),
    successMessage:'Pointers store addresses. In C++ prefer references when you can, pointers when you must.' },

  { id:9021, title:'new and delete', category:'C++', type:'cpp',
    question:'Allocate an int with new (value 42), then free it with delete.',
    hints:['new allocates heap memory','delete frees it','int* p = new int(42); delete p;'],
    validate: vHas(['new int','delete']),
    successMessage:'Every new needs matching delete. Prefer smart pointers to avoid leaks.' },

  { id:9022, title:'Class', category:'C++', type:'cpp',
    question:'Declare a class "Player" with public string "name" and int "level".',
    hints:['class Name { access: members };','public: makes accessible','class Player { public: std::string name; int level; };'],
    validate: vHas(['class Player','public','name','level']),
    successMessage:'Classes default to private, structs to public. Use public for interface, private for internals.' },

  { id:9023, title:'Constructor', category:'C++', type:'cpp',
    question:'Add a constructor to Player that takes a string and assigns it to name.',
    hints:['Same name as class, no return type','Initializes members','Player(std::string n) { name = n; }'],
    validate: vHas(['Player(','name']),
    successMessage:'Use initializer lists for efficiency: Player(string n) : name(n) {}.' },

  { id:9024, title:'Destructor', category:'C++', type:'cpp',
    question:'Add a destructor to Player that prints "Destroyed".',
    hints:['~ClassName()','Called on object destruction','~Player() { cout << "Destroyed"; }'],
    validate: vHas(['~Player','cout']),
    successMessage:'Destructors clean up resources. Rule of Three: if you define one, also define copy ctor and copy assignment.' },

  { id:9025, title:'Inheritance', category:'C++', type:'cpp',
    question:'Create class "Warrior" publicly inheriting from Player with int "attack".',
    hints:[': public Base to inherit','Warrior gets Player\'s members','class Warrior : public Player { public: int attack; };'],
    validate: vHas(['class Warrior',': public Player','attack']),
    successMessage:'C++ supports multiple inheritance. public = "is-a". Protected/private restrict base access.' },

  { id:9026, title:'Virtual Function', category:'C++', type:'cpp',
    question:'Add a virtual "attack" method to Player that prints "Player attacks".',
    hints:['virtual enables polymorphism','Override in derived classes','virtual void attack() { cout << "Player attacks"; }'],
    validate: vHas(['virtual','void attack','cout']),
    successMessage:'virtual enables runtime polymorphism. Without it, base version always runs through base pointer.' },

  { id:9027, title:'Abstract Class', category:'C++', type:'cpp',
    question:'Create abstract class "Shape" with pure virtual method "area" returning double.',
    hints:['= 0 makes it pure virtual','Class becomes abstract','virtual double area() = 0;'],
    validate: vHas(['class Shape','virtual','double area','= 0']),
    successMessage:'Pure virtual (= 0) makes class abstract. Derived classes must implement all pure virtuals.' },

  { id:9028, title:'Template Function', category:'C++', type:'cpp',
    question:'Write a template function "maximum" returning the larger of two values.',
    hints:['template<typename T>','Use > to compare','template<typename T> T maximum(T a, T b) { return (a > b) ? a : b; }'],
    validate: vHas(['template','typename T','maximum']),
    successMessage:'Templates generate type-specific code at compile time. Foundation of generic programming.' },

  { id:9029, title:'Template Class', category:'C++', type:'cpp',
    question:'Write template class "Box" storing a single value of type T.',
    hints:['template<typename T> before class','T is placeholder type','template<typename T> class Box { public: T value; };'],
    validate: vHas(['template','typename T','class Box','T value']),
    successMessage:'Class templates create generic containers. vector, map, optional are all class templates.' },

  { id:9030, title:'Namespace', category:'C++', type:'cpp',
    question:'Declare namespace "game" containing int "score" set to 0.',
    hints:['namespace name { contents }','Prevents name collisions','namespace game { int score = 0; }'],
    validate: vHas(['namespace game','int score','0']),
    successMessage:'Access with game::score. Avoid "using namespace" in headers. std:: is the standard namespace.' },

  { id:9031, title:'Try-Catch', category:'C++', type:'cpp',
    question:'Write a try-catch catching std::exception and printing e.what().',
    hints:['try { } catch (type& e) { }','Catch by const reference','try { } catch (const std::exception& e) { cout << e.what(); }'],
    validate: vHas(['try','catch','exception','what()']),
    successMessage:'Catch by const reference to avoid slicing. C++ can throw any type but std::exception hierarchy is standard.' },

  { id:9032, title:'Enum Class', category:'C++', type:'cpp',
    question:'Declare enum class "Color" with Red, Green, Blue.',
    hints:['enum class is strongly typed','Values are scoped','enum class Color { Red, Green, Blue };'],
    validate: vHas(['enum class','Color','Red','Green','Blue']),
    successMessage:'enum class (C++11) is scoped and won\'t implicitly convert to int. Plain enum is unscoped.' },

  { id:9033, title:'Smart Pointer', category:'C++', type:'cpp',
    question:'Create a unique_ptr to an int with value 42.',
    hints:['#include <memory>','make_unique creates it','auto p = std::make_unique<int>(42);'],
    validate: vHas(['unique_ptr','make_unique','42']),
    successMessage:'unique_ptr owns exclusively, shared_ptr allows sharing. Smart pointers eliminate most memory leaks.' },

  { id:9034, title:'Lambda', category:'C++', type:'cpp',
    question:'Create a lambda "add" taking two ints, returning their sum.',
    hints:['[] (captures) (params) { body }','auto stores lambdas','auto add = [](int a, int b) { return a + b; };'],
    validate: vHas(['auto add','[]','return']),
    successMessage:'Lambdas (C++11): [] no capture, [&] by ref, [=] by value. Essential for STL algorithms.' },

  { id:9035, title:'STL sort', category:'C++', type:'cpp',
    question:'Sort a vector "nums" in ascending order using std::sort.',
    hints:['#include <algorithm>','Takes begin and end iterators','std::sort(nums.begin(), nums.end());'],
    validate: vHas(['sort','nums.begin()','nums.end()']),
    successMessage:'std::sort is O(n log n). Pass a custom comparator as third arg. <algorithm> has dozens of useful functions.' },
];
