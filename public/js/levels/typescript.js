import { vHas } from './helpers.js';

export const TYPESCRIPT_LEVELS = [
  { id:10001, title:'Type Annotation', category:'TypeScript', type:'typescript',
    question:'Declare a variable "name" with type string and value "Alex".',
    hints:['Add : type after the variable name','let name: string','let name: string = "Alex";'],
    validate: vHas(['let','name',': string','Alex']),
    successMessage:'TypeScript adds static types to JavaScript. Types are checked at compile time, removed at runtime.' },

  { id:10002, title:'Number Type', category:'TypeScript', type:'typescript',
    question:'Declare a variable "score" with type number set to 100.',
    hints:['number covers all numeric types','let name: number = value','let score: number = 100;'],
    validate: vHas(['let','score',': number','100']),
    successMessage:'TypeScript has number (all numbers), bigint (arbitrary precision). No separate int/float.' },

  { id:10003, title:'Boolean Type', category:'TypeScript', type:'typescript',
    question:'Declare a variable "isActive" with type boolean set to true.',
    hints:['boolean for true/false','let name: boolean = value','let isActive: boolean = true;'],
    validate: vHas(['isActive',': boolean','true']),
    successMessage:'boolean only accepts true/false. TypeScript catches type errors that JS wouldn\'t.' },

  { id:10004, title:'Array Type', category:'TypeScript', type:'typescript',
    question:'Declare an array of numbers called "scores" with values 90, 85, 92.',
    hints:['number[] or Array<number>','let name: type[] = [values]','let scores: number[] = [90, 85, 92];'],
    validate: vHas(['scores',': number[]','90','85','92']),
    successMessage:'Two syntaxes: number[] and Array<number>. Both are equivalent. number[] is more common.' },

  { id:10005, title:'Function Types', category:'TypeScript', type:'typescript',
    question:'Write a function "add" with typed parameters (a: number, b: number) returning number.',
    hints:['Add types to params and return','function name(p: type): returnType','function add(a: number, b: number): number { return a + b; }'],
    validate: vHas(['function add','a: number','b: number',': number','return']),
    successMessage:'Explicit return types catch errors. TypeScript can often infer them, but explicit is clearer.' },

  { id:10006, title:'Interface', category:'TypeScript', type:'typescript',
    question:'Create an interface "User" with name (string) and age (number) properties.',
    hints:['interface Name { prop: type; }','Defines object shape','interface User { name: string; age: number; }'],
    validate: vHas(['interface User','name: string','age: number']),
    successMessage:'Interfaces define object shapes. They\'re erased at runtime — zero cost. Use for contracts.' },

  { id:10007, title:'Type Alias', category:'TypeScript', type:'typescript',
    question:'Create a type alias "ID" that can be either string or number.',
    hints:['type Name = types','| for union types','type ID = string | number;'],
    validate: vHas(['type ID','string','|','number']),
    successMessage:'type aliases can represent unions, intersections, and complex types. Interfaces can only describe objects.' },

  { id:10008, title:'Optional Property', category:'TypeScript', type:'typescript',
    question:'Create an interface "Config" with required "host" (string) and optional "port" (number).',
    hints:['? makes a property optional','prop?: type','interface Config { host: string; port?: number; }'],
    validate: vHas(['interface Config','host: string','port?',': number']),
    successMessage:'Optional properties (?) can be undefined. Use ?? (nullish coalescing) for defaults.' },

  { id:10009, title:'Union Type', category:'TypeScript', type:'typescript',
    question:'Declare a variable "result" that can be either a string or null.',
    hints:['| creates union types','let name: type1 | type2','let result: string | null = null;'],
    validate: vHas(['result',': string | null']),
    successMessage:'Union types express "either/or". Use type guards (typeof, instanceof) to narrow.' },

  { id:10010, title:'Type Guard', category:'TypeScript', type:'typescript',
    question:'Write an if block using typeof to check if a variable "value" is a string before calling toUpperCase().',
    hints:['typeof checks runtime type','TypeScript narrows inside the block','if (typeof value === "string") { value.toUpperCase(); }'],
    validate: vHas(['typeof','value','string','toUpperCase']),
    successMessage:'Type guards narrow union types. typeof, instanceof, "in" operator, and custom guards all work.' },

  { id:10011, title:'Enum', category:'TypeScript', type:'typescript',
    question:'Create an enum "Direction" with Up, Down, Left, Right.',
    hints:['enum Name { values }','Values auto-number from 0','enum Direction { Up, Down, Left, Right }'],
    validate: vHas(['enum Direction','Up','Down','Left','Right']),
    successMessage:'Enums compile to JS objects. Use const enum for inlined values (better performance).' },

  { id:10012, title:'Tuple Type', category:'TypeScript', type:'typescript',
    question:'Declare a tuple "point" of type [number, number] with values 10 and 20.',
    hints:['Tuples are fixed-length typed arrays','let name: [type, type]','let point: [number, number] = [10, 20];'],
    validate: vHas(['point',': [number, number]','10','20']),
    successMessage:'Tuples have fixed length and types at each position. Great for pairs and triples.' },

  { id:10013, title:'Readonly', category:'TypeScript', type:'typescript',
    question:'Create an interface "Point" with readonly x and y number properties.',
    hints:['readonly prevents reassignment','readonly prop: type','interface Point { readonly x: number; readonly y: number; }'],
    validate: vHas(['interface Point','readonly','x: number','y: number']),
    successMessage:'readonly prevents mutation after creation. Use Readonly<T> utility to make all props readonly.' },

  { id:10014, title:'Generic Function', category:'TypeScript', type:'typescript',
    question:'Write a generic function "identity" that takes a value of type T and returns it.',
    hints:['<T> after function name','T is a type parameter','function identity<T>(value: T): T { return value; }'],
    validate: vHas(['function identity<T>','value: T',': T','return value']),
    successMessage:'Generics let you write type-safe code that works with any type. T is conventional but any name works.' },

  { id:10015, title:'Generic Interface', category:'TypeScript', type:'typescript',
    question:'Create a generic interface "Response<T>" with data (T) and status (number) properties.',
    hints:['interface Name<T> { }','T is the generic parameter','interface Response<T> { data: T; status: number; }'],
    validate: vHas(['interface Response<T>','data: T','status: number']),
    successMessage:'Generic interfaces model reusable shapes. Response<User>, Response<string> — one definition, many uses.' },

  { id:10016, title:'Intersection Type', category:'TypeScript', type:'typescript',
    question:'Create a type "Admin" that combines User and { role: string } using intersection.',
    hints:['& combines types','type Name = A & B','type Admin = User & { role: string };'],
    validate: vHas(['type Admin','User','&','role: string']),
    successMessage:'Intersection types combine all properties. Union (|) = either, intersection (&) = both.' },

  { id:10017, title:'Literal Types', category:'TypeScript', type:'typescript',
    question:'Create a type "Direction" that only allows "left", "right", "up", or "down".',
    hints:['Use literal strings in union','type Name = "a" | "b"','type Direction = "left" | "right" | "up" | "down";'],
    validate: vHas(['type Direction','left','right','up','down']),
    successMessage:'Literal types restrict to exact values. Great for state machines and config options.' },

  { id:10018, title:'Type Assertion', category:'TypeScript', type:'typescript',
    question:'Use "as" to assert that a variable "input" is a string.',
    hints:['value as Type','Tells compiler you know better','const text = input as string;'],
    validate: vHas(['as string']),
    successMessage:'Assertions override the compiler. Use sparingly — they bypass safety. Prefer type guards.' },

  { id:10019, title:'Utility Types - Partial', category:'TypeScript', type:'typescript',
    question:'Use Partial<User> to create a type where all User properties are optional.',
    hints:['Partial makes all props optional','type Name = Partial<Type>','type UpdateUser = Partial<User>;'],
    validate: vHas(['Partial<User>']),
    successMessage:'Built-in utilities: Partial, Required, Pick, Omit, Record, Readonly. Essential for real code.' },

  { id:10020, title:'Utility Types - Pick', category:'TypeScript', type:'typescript',
    question:'Use Pick to create a type "UserName" with only the "name" property from User.',
    hints:['Pick selects specific props','Pick<Type, "key">','type UserName = Pick<User, "name">;'],
    validate: vHas(['Pick<User','name']),
    successMessage:'Pick extracts specific properties. Omit does the opposite — excludes specified keys.' },

  { id:10021, title:'Record Type', category:'TypeScript', type:'typescript',
    question:'Create a Record type mapping string keys to number values, called "Scores".',
    hints:['Record<Keys, Values>','Creates key-value type','type Scores = Record<string, number>;'],
    validate: vHas(['Record<string, number>']),
    successMessage:'Record<K, V> creates an object type with keys K and values V. Cleaner than index signatures.' },

  { id:10022, title:'Class with Types', category:'TypeScript', type:'typescript',
    question:'Create a class "Player" with private name (string) and public getScore() returning number.',
    hints:['Access modifiers: public, private, protected','Type the return value','class Player { private name: string; public getScore(): number { return 0; } }'],
    validate: vHas(['class Player','private','name: string','getScore',': number']),
    successMessage:'TS classes have access modifiers. Parameter properties shorthand: constructor(private name: string) {}.' },

  { id:10023, title:'Abstract Class', category:'TypeScript', type:'typescript',
    question:'Create an abstract class "Shape" with an abstract method "area" returning number.',
    hints:['abstract keyword on class and method','No body for abstract methods','abstract class Shape { abstract area(): number; }'],
    validate: vHas(['abstract class Shape','abstract area',': number']),
    successMessage:'Abstract classes can\'t be instantiated. Mix of contracts (abstract) and implementations (concrete).' },

  { id:10024, title:'keyof Operator', category:'TypeScript', type:'typescript',
    question:'Use keyof to create a type "UserKeys" representing all keys of the User interface.',
    hints:['keyof extracts key names','type Name = keyof Type','type UserKeys = keyof User;'],
    validate: vHas(['keyof User']),
    successMessage:'keyof returns a union of property names. Combine with generics: function get<T, K extends keyof T>().' },

  { id:10025, title:'typeof in Types', category:'TypeScript', type:'typescript',
    question:'Use typeof to create a type from an existing variable "config".',
    hints:['typeof extracts a variable\'s type','type Name = typeof variable','type Config = typeof config;'],
    validate: vHas(['typeof config']),
    successMessage:'typeof in type position extracts the type of a value. Great for deriving types from constants.' },

  { id:10026, title:'Mapped Type', category:'TypeScript', type:'typescript',
    question:'Create a mapped type "ReadonlyUser" that makes all User props readonly.',
    hints:['[K in keyof T]: T[K]','Iterate over keys','type ReadonlyUser = { readonly [K in keyof User]: User[K] };'],
    validate: vHas(['[','in keyof','User']),
    successMessage:'Mapped types transform existing types key by key. Partial, Required, Readonly are built on this.' },

  { id:10027, title:'Conditional Type', category:'TypeScript', type:'typescript',
    question:'Create a type "IsString<T>" that returns "yes" if T is string, "no" otherwise.',
    hints:['T extends U ? A : B','Ternary at type level','type IsString<T> = T extends string ? "yes" : "no";'],
    validate: vHas(['type IsString<T>','extends string','?']),
    successMessage:'Conditional types enable type-level logic. Combined with infer, they unlock powerful patterns.' },

  { id:10028, title:'Template Literal Types', category:'TypeScript', type:'typescript',
    question:'Create a type "EventName" that produces "onClick" | "onHover" from "Click" | "Hover".',
    hints:['Use template literal syntax in types','`on${T}`','type EventName<T extends string> = `on${T}`;'],
    validate: vHas(['type EventName','`on${']),
    successMessage:'Template literal types (TS 4.1+) manipulate string types. Combine with unions for powerful patterns.' },

  { id:10029, title:'Discriminated Union', category:'TypeScript', type:'typescript',
    question:'Create types "Circle" and "Square" with a shared "kind" property, unioned as "Shape".',
    hints:['Each type has a literal kind','type Shape = Circle | Square','type Circle = { kind: "circle"; radius: number }; type Square = { kind: "square"; side: number }; type Shape = Circle | Square;'],
    validate: vHas(['kind','"circle"','"square"','Circle | Square']),
    successMessage:'Discriminated unions use a shared literal property for narrowing. switch(shape.kind) works perfectly.' },

  { id:10030, title:'Never Type', category:'TypeScript', type:'typescript',
    question:'Write a function "fail" that throws an error and has return type never.',
    hints:['never means function never returns','Throwing always produces never','function fail(msg: string): never { throw new Error(msg); }'],
    validate: vHas(['function fail',': never','throw']),
    successMessage:'never = unreachable. Functions that always throw or loop forever return never. Used for exhaustiveness checks.' },

  { id:10031, title:'Index Signature', category:'TypeScript', type:'typescript',
    question:'Create an interface "Dictionary" with string keys and string values using an index signature.',
    hints:['[key: string]: type','Dynamic keys','interface Dictionary { [key: string]: string; }'],
    validate: vHas(['interface Dictionary','[key: string]',': string']),
    successMessage:'Index signatures allow dynamic keys. Record<string, string> is often a cleaner alternative.' },

  { id:10032, title:'Satisfies Operator', category:'TypeScript', type:'typescript',
    question:'Use "satisfies" to check that a config object matches a Config type without widening.',
    hints:['value satisfies Type','TS 4.9+ feature','const config = { host: "localhost", port: 3000 } satisfies Config;'],
    validate: vHas(['satisfies']),
    successMessage:'satisfies validates without changing the inferred type. Gives you both type checking and precise inference.' },

  { id:10033, title:'Async Return Type', category:'TypeScript', type:'typescript',
    question:'Write an async function "fetchData" that returns Promise<string>.',
    hints:['async functions return Promises','Promise<Type>','async function fetchData(): Promise<string> { return "data"; }'],
    validate: vHas(['async function fetchData','Promise<string>','return']),
    successMessage:'async functions always return Promises. Annotate for clarity: Promise<User[]>, Promise<void>, etc.' },

  { id:10034, title:'Type Narrowing with in', category:'TypeScript', type:'typescript',
    question:'Use the "in" operator to check if a "name" property exists in a variable "obj".',
    hints:['"prop" in object','Narrows to types with that prop','if ("name" in obj) { console.log(obj.name); }'],
    validate: vHas(['"name" in','obj']),
    successMessage:'"in" narrows to types that have that property. Works great with discriminated unions.' },

  { id:10035, title:'Decorator', category:'TypeScript', type:'typescript',
    question:'Write a class decorator "sealed" that freezes the constructor\'s prototype.',
    hints:['Decorators are functions','Takes constructor param','function sealed(constructor: Function) { Object.freeze(constructor.prototype); }'],
    validate: vHas(['function sealed','constructor','Object.freeze','prototype']),
    successMessage:'Decorators (stage 3) modify classes, methods, properties. Enable with experimentalDecorators or TS 5.0+ native.' },
];
