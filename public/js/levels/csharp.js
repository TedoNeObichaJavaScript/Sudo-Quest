// ═══════════════════════════════════════════════════════════════
//  C# levels
//  Copyright (c) 2024-2026 TedoNeObichaJavaScript. All Rights Reserved.
// ═══════════════════════════════════════════════════════════════
import { vHas } from './helpers.js';

export const CSHARP_LEVELS = [
  { id:5001, title:'Hello World', category:'C#', type:'csharp',
    question:'Print the text "Hello, World!" to the console.',
    hints:['Console.WriteLine() prints to the console','Text goes in double quotes','Console.WriteLine("Hello, World!");'],
    validate: vHas(['Console.WriteLine','Hello, World']),
    successMessage:'Console.WriteLine() adds a newline. Console.Write() does not. These are your debugging basics.' },

  { id:5002, title:'Variable Declaration', category:'C#', type:'csharp',
    question:'Declare a string variable called "name" and assign it the value "Alex".',
    hints:['C# requires type declarations','string keyword for text','string name = "Alex";'],
    validate: vHas(['string','name','=','"Alex"']),
    successMessage:'C# is statically typed: string, int, double, bool, char. "var" lets the compiler infer the type.' },

  { id:5003, title:'Integer Variable', category:'C#', type:'csharp',
    question:'Declare an integer variable called "score" and set it to 100.',
    hints:['int is for whole numbers','int variableName = value;','int score = 100;'],
    validate: vHas(['int','score','100']),
    successMessage:'int holds whole numbers (-2B to +2B). Use long for bigger, short for smaller, byte for 0-255.' },

  { id:5004, title:'Double Variable', category:'C#', type:'csharp',
    question:'Declare a decimal number variable called "price" and set it to 9.99.',
    hints:['double holds decimal numbers','double variableName = value;','double price = 9.99;'],
    validate: vHas(['double','price','9.99']),
    successMessage:'double for most decimals. float for less precision (add f: 9.99f). decimal for money (add m: 9.99m).' },

  { id:5005, title:'Boolean Variable', category:'C#', type:'csharp',
    question:'Declare a boolean variable called "isActive" and set it to true.',
    hints:['bool holds true or false','lowercase true/false','bool isActive = true;'],
    validate: vHas(['bool','isActive','true']),
    successMessage:'Booleans drive all control flow: if statements, while loops, conditions.' },

  { id:5006, title:'String Interpolation', category:'C#', type:'csharp',
    question:'Create a variable name set to "World", then use string interpolation to build a greeting message containing the name.',
    hints:['$ before the string enables interpolation','{variable} embeds values','string name = "World"; string msg = $"Hello, {name}";'],
    validate: vHas(['$"','{']),
    successMessage:'$"text {expr}" is C# string interpolation. Cleaner than concatenation: "Hello, " + name.' },

  { id:5007, title:'If Statement', category:'C#', type:'csharp',
    question:'Write an if statement that checks if a score is greater than 50, and prints "Pass" if true.',
    hints:['Condition goes in parentheses','Code block in curly braces','if (score > 50) { Console.WriteLine("Pass"); }'],
    validate: vHas(['if','Console.WriteLine']),
    successMessage:'If statements in C# require parentheses around the condition. Braces are optional for single lines but recommended.' },

  { id:5008, title:'If-Else', category:'C#', type:'csharp',
    question:'Write an if-else: print "Adult" when age is 18 or above, and "Minor" otherwise.',
    hints:['else follows the closing brace','else { alternative code }','if (age >= 18) { Console.WriteLine("Adult"); } else { Console.WriteLine("Minor"); }'],
    validate: vHas(['if','else','Console.WriteLine']),
    successMessage:'Chain with else if for multiple conditions. Switch statements work for discrete values.' },

  { id:5009, title:'For Loop', category:'C#', type:'csharp',
    question:'Write a for loop that prints the numbers 1 through 5 to the console.',
    hints:['for (init; condition; update)','i++ increments by 1','for (int i = 1; i <= 5; i++) { Console.WriteLine(i); }'],
    validate: vHas(['for','int i','i++','Console.WriteLine']),
    successMessage:'For loops in C# are identical to JavaScript/Java. Use foreach for collections.' },

  { id:5010, title:'While Loop', category:'C#', type:'csharp',
    question:'Write a while loop that counts from 1 to 3.',
    hints:['while (condition) { body }','Don\'t forget to increment','int i = 1; while (i <= 3) { Console.WriteLine(i); i++; }'],
    validate: vHas(['while','Console.WriteLine']),
    successMessage:'while loops run as long as the condition is true. Always ensure the condition eventually becomes false.' },

  { id:5011, title:'Array', category:'C#', type:'csharp',
    question:'Create a string array called "fruits" containing "apple", "banana", and "cherry".',
    hints:['Arrays use [] after the type','Initialize with { } braces','string[] fruits = { "apple", "banana", "cherry" };'],
    validate: vHas(['string[]','fruits','apple','banana','cherry']),
    successMessage:'Arrays have fixed size in C#. Use List<T> for dynamic sizing. Access with [0], [1], etc.' },

  { id:5012, title:'Array Access', category:'C#', type:'csharp',
    question:'Print the first element of an array called "fruits" to the console.',
    hints:['Arrays are zero-indexed','[0] is the first element','Console.WriteLine(fruits[0]);'],
    validate: vHas(['fruits[0]','Console.WriteLine']),
    successMessage:'C# arrays are zero-indexed like most languages. .Length gives the count. Out of bounds throws an exception.' },

  { id:5013, title:'Foreach Loop', category:'C#', type:'csharp',
    question:'Use a foreach loop to print every item in a collection to the console.',
    hints:['foreach iterates through collections','foreach (type item in collection)','foreach (string fruit in fruits) { Console.WriteLine(fruit); }'],
    validate: vHas(['foreach','in','Console.WriteLine']),
    successMessage:'foreach is cleaner than for when you don\'t need the index. Works with any IEnumerable.' },

  { id:5014, title:'List<T>', category:'C#', type:'csharp',
    question:'Create a generic List of strings called "names" initialized with "Alice" and "Bob".',
    hints:['List<T> is a generic dynamic array','new List<type> { items }','List<string> names = new List<string> { "Alice", "Bob" };'],
    validate: vHas(['List<string>','new','Alice','Bob']),
    successMessage:'List<T> is the most-used collection. .Add(), .Remove(), .Contains(), .Count. Always prefer over arrays.' },

  { id:5015, title:'List Add', category:'C#', type:'csharp',
    question:'Add the string "Charlie" to an existing List called "names".',
    hints:['.Add() appends an element','names.Add(item);','names.Add("Charlie");'],
    validate: vHas(['.Add(','Charlie']),
    successMessage:'.Add() appends. .Insert(index, item) inserts at position. .AddRange() adds multiple.' },

  { id:5016, title:'Method Declaration', category:'C#', type:'csharp',
    question:'Declare a static method called "Greet" that takes no parameters and prints "Hello!" to the console.',
    hints:['static void Name() for no return value','void means returns nothing','static void Greet() { Console.WriteLine("Hello!"); }'],
    validate: vHas(['static','void','Greet','Console.WriteLine']),
    successMessage:'Methods organize code into reusable blocks. static means it belongs to the class, not an instance.' },

  { id:5017, title:'Method with Return', category:'C#', type:'csharp',
    question:'Create a static method called "Add" that takes two integer parameters and returns their sum.',
    hints:['Return type replaces void','return sends value back','static int Add(int a, int b) { return a + b; }'],
    validate: vHas(['static int','return','a + b']),
    successMessage:'The return type must match what you return. int method returns int, string returns string.' },

  { id:5018, title:'Method with Parameters', category:'C#', type:'csharp',
    question:'Create a static method called "Greet" that accepts a string name parameter and returns a greeting using string interpolation.',
    hints:['Parameters go in parentheses','Type and name for each parameter','static string Greet(string name) { return $"Hello, {name}!"; }'],
    validate: vHas(['static string','Greet','string name','return']),
    successMessage:'Parameters have explicit types. C# supports default values: string Greet(string name = "World").' },

  { id:5019, title:'Class Declaration', category:'C#', type:'csharp',
    question:'Declare a class called "Player" with two public fields: a string called "Name" and an int called "Level".',
    hints:['class keyword creates a class','public makes members accessible','class Player { public string Name; public int Level; }'],
    validate: vHas(['class','Player','public','string Name','int Level']),
    successMessage:'Classes are blueprints. Use properties (get;set;) instead of public fields in real code.' },

  { id:5020, title:'Constructor', category:'C#', type:'csharp',
    question:'Add a constructor to the Player class that accepts a string parameter and assigns it to the Name field.',
    hints:['Constructor has same name as class','No return type','public Player(string name) { Name = name; }'],
    validate: vHas(['class','Player','public Player(','Name = name']),
    successMessage:'Constructors initialize objects. Called with "new": var p = new Player("Alex");' },

  { id:5021, title:'Properties', category:'C#', type:'csharp',
    question:'Create a class called "User" with auto-properties for Name (string) and Age (int) using get and set.',
    hints:['Properties use { get; set; }','Better than public fields','public string Name { get; set; }'],
    validate: vHas(['get;','set;']),
    successMessage:'Properties encapsulate fields. { get; set; } is auto-implemented. { get; } is read-only.' },

  { id:5022, title:'New Object', category:'C#', type:'csharp',
    question:'Create a new Player object stored in variable "p", then set its Name property to "Hero".',
    hints:['new creates instances','Dot notation accesses members','Player p = new Player(); p.Name = "Hero";'],
    validate: vHas(['new','Player','p.Name','Hero']),
    successMessage:'"new" allocates and initializes. Object initializer: new Player { Name = "Hero" }.' },

  { id:5023, title:'Inheritance', category:'C#', type:'csharp',
    question:'Create a class "Warrior" that inherits from Player, adding a public int field called "Attack".',
    hints:[': means "inherits from"','Warrior extends Player','class Warrior : Player { public int Attack; }'],
    validate: vHas(['class Warrior',': Player','Attack']),
    successMessage:'C# supports single inheritance. Use interfaces for multiple. : BaseClass to inherit.' },

  { id:5024, title:'Interface', category:'C#', type:'csharp',
    question:'Declare an interface called "IMovable" with a single method signature "Move" that returns void.',
    hints:['Interfaces define contracts','Convention: prefix with I','interface IMovable { void Move(); }'],
    validate: vHas(['interface','IMovable','void Move']),
    successMessage:'Interfaces define what a class must implement. A class can implement multiple interfaces.' },

  { id:5025, title:'Try-Catch', category:'C#', type:'csharp',
    question:'Write a try-catch block that attempts to parse the string "abc" as an integer, catching and printing the exception message.',
    hints:['try contains risky code','catch handles exceptions','try { } catch (Exception e) { Console.WriteLine(e.Message); }'],
    validate: vHas(['try','catch','Exception']),
    successMessage:'try-catch prevents crashes. Catch specific exceptions (FormatException) before generic Exception.' },

  { id:5026, title:'Switch Statement', category:'C#', type:'csharp',
    question:'Write a switch statement on a variable "day" with a case for "Mon" that prints "Monday", and a default case.',
    hints:['switch matches against cases','break exits the switch','switch (day) { case "Mon": ... break; default: break; }'],
    validate: vHas(['switch','case','break','default']),
    successMessage:'Switch is cleaner than long if-else chains. C# 8+ has switch expressions: day switch { "Mon" => "Monday" }.' },

  { id:5027, title:'Ternary Operator', category:'C#', type:'csharp',
    question:'Use the ternary operator to set a string variable "status" to "pass" if score is above 50, or "fail" otherwise.',
    hints:['condition ? ifTrue : ifFalse','Shorthand for if-else','string status = (score > 50) ? "pass" : "fail";'],
    validate: vHas(['?',':','pass','fail']),
    successMessage:'Ternary is a one-line if-else for assignments. Keep it simple — use full if-else for complex logic.' },

  { id:5028, title:'Null Check', category:'C#', type:'csharp',
    question:'Write an if statement that checks if a variable "name" is not null before printing it.',
    hints:['null means "no value"','!= null checks existence','if (name != null) { Console.WriteLine(name); }'],
    validate: vHas(['!= null','Console.WriteLine']),
    successMessage:'Null is a billion-dollar mistake. Use nullable types (string?), null-coalescing (??), and null-conditional (?.).' },

  { id:5029, title:'Null Coalescing', category:'C#', type:'csharp',
    question:'Use the null coalescing operator to assign "name" to a variable "display", defaulting to "Unknown" if name is null.',
    hints:['?? provides a default when null','left ?? defaultValue','string display = name ?? "Unknown";'],
    validate: vHas(['??','Unknown']),
    successMessage:'?? returns the left value if not null, otherwise the right. ??= assigns only if null.' },

  { id:5030, title:'String Methods', category:'C#', type:'csharp',
    question:'Convert a string variable "name" to both uppercase and lowercase, storing each result in a separate variable.',
    hints:['.ToUpper() converts to uppercase','.ToLower() to lowercase','string upper = name.ToUpper(); string lower = name.ToLower();'],
    validate: vHas(['.ToUpper()','.ToLower()']),
    successMessage:'String methods: .Trim(), .Split(), .Replace(), .Contains(), .StartsWith(), .Substring().' },

  { id:5031, title:'Dictionary', category:'C#', type:'csharp',
    question:'Create a Dictionary that maps string keys to int values, initialized with "Alice" mapped to 30.',
    hints:['Dictionary<TKey, TValue>','Key-value pairs in { }','Dictionary<string, int> ages = new Dictionary<string, int> { {"Alice", 30} };'],
    validate: vHas(['Dictionary<string, int>','Alice','30']),
    successMessage:'Dictionary is a key-value store. .Add(), .Remove(), .ContainsKey(), .TryGetValue(). O(1) lookup.' },

  { id:5032, title:'LINQ Where', category:'C#', type:'csharp',
    question:'Use LINQ to filter a numbers collection, keeping only the even numbers.',
    hints:['LINQ = Language Integrated Query','.Where() filters collections','var evens = numbers.Where(n => n % 2 == 0);'],
    validate: vHas(['.Where(','=> ']),
    successMessage:'LINQ transforms collections: .Where() .Select() .OrderBy() .First() .Any() .Count(). Extremely powerful.' },

  { id:5033, title:'LINQ Select', category:'C#', type:'csharp',
    question:'Use LINQ to transform a numbers collection by doubling each value.',
    hints:['.Select() transforms each element','Like .map() in JavaScript','var doubled = numbers.Select(n => n * 2);'],
    validate: vHas(['.Select(','=> ']),
    successMessage:'.Select() is C#\'s map. Chain LINQ: numbers.Where(n => n > 0).Select(n => n * 2).ToList().' },

  { id:5034, title:'Async Method', category:'C#', type:'csharp',
    question:'Declare an async method called "FetchData" that returns a Task<string> and uses the await keyword inside.',
    hints:['async enables await','Task<T> is the return type','async Task<string> FetchData() { return await GetDataAsync(); }'],
    validate: vHas(['async','Task','await']),
    successMessage:'async/await makes asynchronous code readable. Task = void, Task<T> = returns T.' },

  { id:5035, title:'Enum', category:'C#', type:'csharp',
    question:'Declare an enum called "Direction" with four values: Up, Down, Left, and Right.',
    hints:['enum defines named constants','Comma-separated values','enum Direction { Up, Down, Left, Right }'],
    validate: vHas(['enum','Direction','Up','Down','Left','Right']),
    successMessage:'Enums make code readable: Direction.Up vs magic number 0. Backed by int by default.' },
];
