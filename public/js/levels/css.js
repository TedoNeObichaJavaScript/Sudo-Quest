import { vHas } from './helpers.js';

export const CSS_LEVELS = [
  { id:4001, title:'Color Property', category:'CSS', type:'css',
    question:'Write a CSS rule: h1 { color: red; }',
    hints:['selector { property: value; }','color sets text color','h1 { color: red; }'],
    validate: vHas(['h1','color','red']),
    successMessage:'color sets text color. Use named colors, hex (#ff0000), rgb(), or hsl().' },

  { id:4002, title:'Background Color', category:'CSS', type:'css',
    question:'Set a div\'s background to blue: div { background-color: blue; }',
    hints:['background-color sets the background','Use named colors or hex','div { background-color: blue; }'],
    validate: vHas(['background-color','blue']),
    successMessage:'background-color fills the element\'s box. Shorthand "background" also sets images and gradients.' },

  { id:4003, title:'Font Size', category:'CSS', type:'css',
    question:'Set paragraph font size to 16px: p { font-size: 16px; }',
    hints:['font-size controls text size','px is pixels','p { font-size: 16px; }'],
    validate: vHas(['font-size','16px']),
    successMessage:'Units: px (fixed), em (relative to parent), rem (relative to root), % (percentage). Prefer rem for scalability.' },

  { id:4004, title:'Font Family', category:'CSS', type:'css',
    question:'Set body font to Arial with a fallback: body { font-family: Arial, sans-serif; }',
    hints:['font-family sets the typeface','Include fallback fonts','body { font-family: Arial, sans-serif; }'],
    validate: vHas(['font-family','Arial','sans-serif']),
    successMessage:'Always include a generic fallback (serif, sans-serif, monospace). Use Google Fonts for web fonts.' },

  { id:4005, title:'Text Alignment', category:'CSS', type:'css',
    question:'Center-align text: h1 { text-align: center; }',
    hints:['text-align aligns inline content','center, left, right, justify','h1 { text-align: center; }'],
    validate: vHas(['text-align','center']),
    successMessage:'text-align works on inline content inside a block. For centering blocks, use margin: 0 auto.' },

  { id:4006, title:'Margin', category:'CSS', type:'css',
    question:'Add 20px margin to all sides of a div: div { margin: 20px; }',
    hints:['margin creates space OUTSIDE the element','One value = all four sides','div { margin: 20px; }'],
    validate: vHas(['margin','20px']),
    successMessage:'margin: top right bottom left. margin: 0 auto centers blocks. Vertical margins collapse.' },

  { id:4007, title:'Padding', category:'CSS', type:'css',
    question:'Add 10px padding to a div: div { padding: 10px; }',
    hints:['padding creates space INSIDE the element','Similar syntax to margin','div { padding: 10px; }'],
    validate: vHas(['padding','10px']),
    successMessage:'padding is inside the border, margin is outside. Together they control all spacing.' },

  { id:4008, title:'Border', category:'CSS', type:'css',
    question:'Add a border: div { border: 1px solid black; }',
    hints:['border: width style color','solid, dashed, dotted are common styles','div { border: 1px solid black; }'],
    validate: vHas(['border','solid']),
    successMessage:'border is shorthand for border-width, border-style, border-color. Use border-radius for rounded corners.' },

  { id:4009, title:'Border Radius', category:'CSS', type:'css',
    question:'Round corners with: div { border-radius: 8px; }',
    hints:['border-radius rounds corners','Higher values = more round','div { border-radius: 8px; }'],
    validate: vHas(['border-radius']),
    successMessage:'border-radius: 50% makes a circle. You can set each corner: border-radius: 10px 0 0 10px.' },

  { id:4010, title:'Width & Height', category:'CSS', type:'css',
    question:'Set a div to 200px wide and 100px tall.',
    hints:['width and height set dimensions','width: 200px; height: 100px;','div { width: 200px; height: 100px; }'],
    validate: vHas(['width','height','200px','100px']),
    successMessage:'Fixed sizes can break on small screens. Use max-width and % for responsive designs.' },

  { id:4011, title:'Class Selector', category:'CSS', type:'css',
    question:'Style a class: .card { padding: 16px; border: 1px solid gray; }',
    hints:['Classes use a dot prefix: .name','Most common selector type','.card { padding: 16px; border: 1px solid gray; }'],
    validate: vHas(['.card','padding','border']),
    successMessage:'Classes are reusable — use them for most styling. IDs (#) are for unique elements.' },

  { id:4012, title:'ID Selector', category:'CSS', type:'css',
    question:'Style an ID: #header { background: #333; color: white; }',
    hints:['IDs use a hash prefix: #name','IDs have higher specificity','#header { background: #333; color: white; }'],
    validate: vHas(['#header','background','color']),
    successMessage:'IDs have higher specificity than classes. Use sparingly — prefer classes for styling.' },

  { id:4013, title:'Display Block', category:'CSS', type:'css',
    question:'Make a span behave like a block: span { display: block; }',
    hints:['display controls element flow','block takes full width','span { display: block; }'],
    validate: vHas(['display','block']),
    successMessage:'display: block (full width), inline (flows with text), inline-block (inline + width/height), none (hidden).' },

  { id:4014, title:'Display Flex', category:'CSS', type:'css',
    question:'Create a flex container: .container { display: flex; }',
    hints:['Flexbox is the modern layout tool','display: flex on the parent','.container { display: flex; }'],
    validate: vHas(['display','flex']),
    successMessage:'Flexbox is 1D layout (row or column). Set on parent, children become flex items.' },

  { id:4015, title:'Flex Direction', category:'CSS', type:'css',
    question:'Stack items vertically: .container { display: flex; flex-direction: column; }',
    hints:['flex-direction sets the main axis','row (default) or column','.container { display: flex; flex-direction: column; }'],
    validate: vHas(['display: flex','flex-direction','column']),
    successMessage:'row = horizontal, column = vertical. row-reverse and column-reverse flip the order.' },

  { id:4016, title:'Justify Content', category:'CSS', type:'css',
    question:'Center items horizontally: .container { display: flex; justify-content: center; }',
    hints:['justify-content aligns on the main axis','center, space-between, space-around','.container { display: flex; justify-content: center; }'],
    validate: vHas(['justify-content','center']),
    successMessage:'justify-content: center, space-between, space-around, space-evenly, flex-start, flex-end.' },

  { id:4017, title:'Align Items', category:'CSS', type:'css',
    question:'Center items vertically: .container { display: flex; align-items: center; }',
    hints:['align-items aligns on the cross axis','Works perpendicular to main axis','.container { display: flex; align-items: center; }'],
    validate: vHas(['align-items','center']),
    successMessage:'align-items: center, stretch (default), flex-start, flex-end, baseline.' },

  { id:4018, title:'Flex Gap', category:'CSS', type:'css',
    question:'Add spacing between flex items: .container { display: flex; gap: 16px; }',
    hints:['gap sets space between items','No margin hacks needed','.container { display: flex; gap: 16px; }'],
    validate: vHas(['gap','16px']),
    successMessage:'gap works in Flexbox and Grid. Replaces margin-based spacing. row-gap and column-gap for separate control.' },

  { id:4019, title:'Position Relative', category:'CSS', type:'css',
    question:'Set relative positioning: .box { position: relative; top: 10px; }',
    hints:['relative positions relative to normal flow','top/right/bottom/left offset it','.box { position: relative; top: 10px; }'],
    validate: vHas(['position','relative','top']),
    successMessage:'relative moves an element from its normal spot. The original space is preserved.' },

  { id:4020, title:'Position Absolute', category:'CSS', type:'css',
    question:'Position absolutely: .tooltip { position: absolute; top: 0; right: 0; }',
    hints:['absolute positions relative to nearest positioned parent','Removed from normal flow','.tooltip { position: absolute; top: 0; right: 0; }'],
    validate: vHas(['position','absolute']),
    successMessage:'absolute positions relative to the nearest positioned ancestor. Use with a relative parent.' },

  { id:4021, title:'Box Shadow', category:'CSS', type:'css',
    question:'Add a shadow: .card { box-shadow: 0 2px 8px rgba(0,0,0,0.1); }',
    hints:['box-shadow: x y blur color','rgba allows transparency','.card { box-shadow: 0 2px 8px rgba(0,0,0,0.1); }'],
    validate: vHas(['box-shadow']),
    successMessage:'box-shadow: offset-x offset-y blur spread color. Multiple shadows with comma separation.' },

  { id:4022, title:'Opacity', category:'CSS', type:'css',
    question:'Make an element semi-transparent: .overlay { opacity: 0.5; }',
    hints:['opacity: 0 is invisible, 1 is solid','0.5 is 50% transparent','.overlay { opacity: 0.5; }'],
    validate: vHas(['opacity','0.5']),
    successMessage:'opacity affects the whole element + children. Use rgba/hsla colors for background-only transparency.' },

  { id:4023, title:'Transition', category:'CSS', type:'css',
    question:'Add a smooth transition: .btn { transition: all 0.3s ease; }',
    hints:['transition animates property changes','property duration timing-function','.btn { transition: all 0.3s ease; }'],
    validate: vHas(['transition','0.3s']),
    successMessage:'Transitions animate between states. Specify properties: transition: background 0.3s, color 0.2s.' },

  { id:4024, title:'Hover State', category:'CSS', type:'css',
    question:'Style a hover effect: .btn:hover { background: green; }',
    hints:[':hover activates on mouse over','It\'s a pseudo-class','.btn:hover { background: green; }'],
    validate: vHas([':hover','background']),
    successMessage:'Pseudo-classes: :hover, :focus, :active, :first-child, :nth-child(). They style element states.' },

  { id:4025, title:'CSS Grid', category:'CSS', type:'css',
    question:'Create a grid: .grid { display: grid; grid-template-columns: 1fr 1fr 1fr; }',
    hints:['display: grid enables CSS Grid','fr is a fraction unit','.grid { display: grid; grid-template-columns: 1fr 1fr 1fr; }'],
    validate: vHas(['display: grid','grid-template-columns']),
    successMessage:'CSS Grid is 2D layout. fr units distribute space. repeat(3, 1fr) = 1fr 1fr 1fr.' },

  { id:4026, title:'Grid Gap', category:'CSS', type:'css',
    question:'Add grid spacing: .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }',
    hints:['gap works for grid too','Same as flex gap','.grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }'],
    validate: vHas(['display: grid','gap','20px']),
    successMessage:'gap in Grid replaces the old grid-gap. Use row-gap and column-gap for separate control.' },

  { id:4027, title:'Overflow', category:'CSS', type:'css',
    question:'Handle overflow: .box { overflow: hidden; }',
    hints:['overflow controls what happens when content exceeds size','hidden clips content','.box { overflow: hidden; }'],
    validate: vHas(['overflow','hidden']),
    successMessage:'overflow: visible (default), hidden (clip), scroll (always), auto (scroll when needed).' },

  { id:4028, title:'Z-Index', category:'CSS', type:'css',
    question:'Set stacking order: .modal { position: fixed; z-index: 100; }',
    hints:['z-index controls stacking (front/back)','Higher = on top','position required for z-index to work','.modal { position: fixed; z-index: 100; }'],
    validate: vHas(['z-index','100']),
    successMessage:'z-index only works on positioned elements. Use sparingly — cascading z-index leads to chaos.' },

  { id:4029, title:'Text Transform', category:'CSS', type:'css',
    question:'Make text uppercase: h1 { text-transform: uppercase; }',
    hints:['text-transform changes text case','uppercase, lowercase, capitalize','h1 { text-transform: uppercase; }'],
    validate: vHas(['text-transform','uppercase']),
    successMessage:'text-transform: uppercase, lowercase, capitalize. Better than changing HTML — preserves original text.' },

  { id:4030, title:'Line Height', category:'CSS', type:'css',
    question:'Set line spacing: p { line-height: 1.6; }',
    hints:['line-height controls vertical spacing between lines','Unitless values are multipliers','p { line-height: 1.6; }'],
    validate: vHas(['line-height','1.6']),
    successMessage:'line-height: 1.5-1.8 improves readability. Unitless values scale with font-size.' },

  { id:4031, title:'Max Width', category:'CSS', type:'css',
    question:'Limit width: .container { max-width: 1200px; margin: 0 auto; }',
    hints:['max-width limits how wide an element grows','margin: 0 auto centers it','.container { max-width: 1200px; margin: 0 auto; }'],
    validate: vHas(['max-width','1200px']),
    successMessage:'max-width + margin: 0 auto is the classic centered container pattern. Responsive-friendly.' },

  { id:4032, title:'Cursor Pointer', category:'CSS', type:'css',
    question:'Change cursor on hover: .btn { cursor: pointer; }',
    hints:['cursor changes the mouse icon','pointer shows a hand','.btn { cursor: pointer; }'],
    validate: vHas(['cursor','pointer']),
    successMessage:'cursor: pointer, text, move, not-allowed, grab, default. Provides visual feedback.' },

  { id:4033, title:'Media Query', category:'CSS', type:'css',
    question:'Write a mobile breakpoint: @media (max-width: 768px) { .nav { flex-direction: column; } }',
    hints:['@media creates responsive rules','max-width targets smaller screens','@media (max-width: 768px) { .nav { flex-direction: column; } }'],
    validate: vHas(['@media','max-width','768px']),
    successMessage:'Media queries enable responsive design. Common breakpoints: 480px (mobile), 768px (tablet), 1024px (desktop).' },

  { id:4034, title:'CSS Variables', category:'CSS', type:'css',
    question:'Define a CSS variable: :root { --primary: #00ff41; } h1 { color: var(--primary); }',
    hints:['CSS variables start with --','var() reads the variable',':root { --primary: #00ff41; } h1 { color: var(--primary); }'],
    validate: vHas(['--primary','var(--primary)']),
    successMessage:'CSS custom properties (variables) enable theming and reduce repetition. Define in :root for global scope.' },

  { id:4035, title:'Pseudo-Element', category:'CSS', type:'css',
    question:'Add content before an element: .icon::before { content: "★"; }',
    hints:['::before inserts content before element','content property is required','.icon::before { content: "★"; }'],
    validate: vHas(['::before','content']),
    successMessage:'::before and ::after insert virtual elements. Useful for decorations, icons, and clearfix.' },
];
