import { vOut, vOuts, vVar, vCode, hasOutput, outputEquals, hasVar, hasFunc, noErrors, codeHas, P, F } from './helpers.js';

export const JS_LEVELS = [
  { id:1, title:'Hello, World!', category:'JavaScript', type:'js',
    question:'Use console.log() to print "Hello, World!" to the console.',
    hints:['console.log() prints output to the console.','Put text in quotes inside parentheses: console.log("...")','console.log("Hello, World!")'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return hasOutput(r,'Hello, World!')?P:F('Use console.log("Hello, World!") to print the message.'); },
    successMessage:'console.log() is your best friend for debugging. Use it to inspect values at any point in your code.' },

  { id:2, title:'Declare a Variable', category:'JavaScript', type:'js',
    question:'Create a variable called "hero" and set it to the string "Ada".',
    hints:['Use "let" to declare a variable.','Syntax: let variableName = "value"','let hero = "Ada"'],
    validate: vVar('hero','Ada'),
    successMessage:'Variables store data for later use. "let" declares a variable that can be reassigned. Use "const" for values that won\'t change.' },

  { id:3, title:'Number Variable', category:'JavaScript', type:'js',
    question:'Create a variable called "score" and set it to the number 100.',
    hints:['Numbers don\'t need quotes.','Syntax: let variableName = number','let score = 100'],
    validate: vVar('score',100),
    successMessage:'Numbers in JS are written without quotes. "100" is a string, 100 is a number — they behave differently.' },

  { id:4, title:'Basic Math', category:'JavaScript', type:'js',
    question:'Calculate 42 * 2 and store the result in a variable called "result".',
    hints:['Use * for multiplication.','let result = number * number','let result = 42 * 2'],
    validate: vVar('result',84),
    successMessage:'JS supports +, -, *, /, % (modulo), and ** (exponent) for math operations.' },

  { id:5, title:'String Concatenation', category:'JavaScript', type:'js',
    question:'Create a variable "greeting" by joining "Hello, " and "World!" using the + operator.',
    hints:['Use + to join strings together.','let greeting = "first" + "second"','let greeting = "Hello, " + "World!"'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); const g=r.variables.greeting; return(g&&g.includes('Hello')&&g.includes('World'))?P:F('Create "greeting" by concatenating "Hello, " + "World!".'); },
    successMessage:'The + operator joins strings. For complex strings, use template literals with backticks: `Hello, ${name}!`' },

  { id:6, title:'Template Literals', category:'JavaScript', type:'js',
    question:'Create name = "World", then use a template literal to set greeting = `Hello, ${name}!`. Log greeting.',
    hints:['Template literals use backticks ` instead of quotes.','Use ${variable} inside backticks to embed values.','let name = "World"; let greeting = `Hello, ${name}!`; console.log(greeting)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'`'))return F('Use backticks ` for template literals.'); return hasOutput(r,'Hello, World!')?P:F('Output should be "Hello, World!"'); },
    successMessage:'Template literals (``) let you embed expressions with ${...}. Much cleaner than string concatenation!' },

  { id:7, title:'typeof Operator', category:'JavaScript', type:'js',
    question:'Log the typeof of 42 using console.log(typeof 42).',
    hints:['typeof returns the data type as a string.','Syntax: typeof value','console.log(typeof 42)'],
    validate: vOut('number'),
    successMessage:'typeof returns: "number", "string", "boolean", "object", "undefined", "function". Useful for type checking.' },

  { id:8, title:'Boolean Variable', category:'JavaScript', type:'js',
    question:'Create a variable "isReady" set to true, then log it.',
    hints:['Booleans are true or false (no quotes).','let isReady = true','let isReady = true; console.log(isReady)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return(hasVar(r,'isReady',true)&&hasOutput(r,'true'))?P:F('Set isReady to true and log it.'); },
    successMessage:'Booleans represent true/false. They\'re the foundation of all conditional logic in programming.' },

  { id:9, title:'If Statement', category:'JavaScript', type:'js',
    question:'Write an if statement: if 100 > 50, log "victory".',
    hints:['if runs code only when condition is true.','Syntax: if (condition) { console.log("...") }','if (100 > 50) { console.log("victory") }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'if'))return F('Use an if statement.'); return hasOutput(r,'victory')?P:F('Log "victory" when the condition is true.'); },
    successMessage:'If statements let your code make decisions. The code in { } only runs when the condition is true.' },

  { id:10, title:'If-Else', category:'JavaScript', type:'js',
    question:'Write if-else: if 3 > 10, log "yes", otherwise log "no".',
    hints:['The else block runs when condition is false.','if (condition) { ... } else { ... }','if (3 > 10) { console.log("yes") } else { console.log("no") }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'else'))return F('Use an else block.'); return hasOutput(r,'no')?P:F('Since 3 < 10, the else block should log "no".'); },
    successMessage:'else is your fallback — it runs when the if condition is false. You can chain with "else if" for multiple conditions.' },

  { id:11, title:'Strict Equality', category:'JavaScript', type:'js',
    question:'Check if "code" === "code" and if true, log "match".',
    hints:['=== checks if two values are exactly equal.','if (value1 === value2) { ... }','if ("code" === "code") { console.log("match") }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'==='))return F('Use === (strict equality).'); return hasOutput(r,'match')?P:F('Log "match" when the values are equal.'); },
    successMessage:'Always use === over ==. Triple equals checks both value AND type, preventing subtle bugs like "1" == 1 being true.' },

  { id:12, title:'Logical AND', category:'JavaScript', type:'js',
    question:'Check if (true && true) is truthy, and log "both true".',
    hints:['&& returns true only when BOTH sides are true.','if (condition1 && condition2) { ... }','if (true && true) { console.log("both true") }'],
    validate: vCode('&&','both true','Use the && (AND) operator.'),
    successMessage:'Logical operators: && (AND), || (OR), ! (NOT). They combine conditions for complex decision-making.' },

  { id:13, title:'Logical OR', category:'JavaScript', type:'js',
    question:'Check if (false || true) is truthy, and log "at least one".',
    hints:['|| returns true when AT LEAST ONE side is true.','if (condition1 || condition2) { ... }','if (false || true) { console.log("at least one") }'],
    validate: vCode('||','at least one','Use the || (OR) operator.'),
    successMessage:'|| (OR) is true if either side is true. Short-circuit: if left is true, right isn\'t even evaluated.' },

  { id:14, title:'For Loop', category:'JavaScript', type:'js',
    question:'Write a for loop that prints numbers 1 through 5.',
    hints:['A for loop repeats code a set number of times.','for (let i = start; i <= end; i++) { console.log(i) }','for (let i = 1; i <= 5; i++) { console.log(i) }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,/for\s*\(/))return F('Use a for loop.'); return outputEquals(r,['1','2','3','4','5'])?P:F(`Expected: 1,2,3,4,5. Got: ${(r.consoleLogs||[]).join(',')}`); },
    successMessage:'For loops have 3 parts: init (let i=1), condition (i<=5), update (i++). Perfect when you know the count.' },

  { id:15, title:'While Loop', category:'JavaScript', type:'js',
    question:'Use a while loop to print numbers 1 through 3.',
    hints:['while loops repeat as long as condition is true.','let i = 1; while (i <= 3) { ...; i++ }','let i = 1; while (i <= 3) { console.log(i); i++ }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,/while\s*\(/))return F('Use a while loop.'); return outputEquals(r,['1','2','3'])?P:F(`Expected: 1,2,3. Got: ${(r.consoleLogs||[]).join(',')}`); },
    successMessage:'While loops are great when you don\'t know how many iterations you need. Always update the counter to avoid infinite loops!' },

  { id:16, title:'Countdown', category:'JavaScript', type:'js',
    question:'Use a for loop to count DOWN from 5 to 1.',
    hints:['Use i-- to decrease each iteration.','for (let i = 5; i >= 1; i--)','for (let i = 5; i >= 1; i--) { console.log(i) }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return outputEquals(r,['5','4','3','2','1'])?P:F(`Expected: 5,4,3,2,1. Got: ${(r.consoleLogs||[]).join(',')}`); },
    successMessage:'Loops go any direction! i++ counts up, i-- counts down. You can even use i+=2 to skip.' },

  { id:17, title:'for...of Loop', category:'JavaScript', type:'js',
    question:'Create array ["a","b","c"] and use for...of to log each element.',
    hints:['for...of iterates through array values directly.','for (let item of array) { console.log(item) }','let arr = ["a","b","c"]; for (let x of arr) { console.log(x) }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return outputEquals(r,['a','b','c'])?P:F('Loop through ["a","b","c"] and log each element.'); },
    successMessage:'for...of is the cleanest way to loop arrays. Use for...in for object keys instead.' },

  { id:18, title:'Create an Array', category:'JavaScript', type:'js',
    question:'Create an array "fruits" with: "apple", "banana", "cherry".',
    hints:['Arrays use square brackets with commas.','let arrayName = ["a", "b", "c"]','let fruits = ["apple", "banana", "cherry"]'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); const f=r.variables.fruits; return(Array.isArray(f)&&f.length===3&&f.includes('apple')&&f.includes('banana')&&f.includes('cherry'))?P:F('Create fruits = ["apple", "banana", "cherry"].'); },
    successMessage:'Arrays store ordered lists. They can hold any type: strings, numbers, objects, even other arrays!' },

  { id:19, title:'Array Access', category:'JavaScript', type:'js',
    question:'Create ["cat","dog","fish"] and log the FIRST element (index 0).',
    hints:['Arrays are zero-indexed: first element is [0].','console.log(arrayName[0])','let pets = ["cat","dog","fish"]; console.log(pets[0])'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return hasOutput(r,'cat',true)?P:F('Use [0] to access the first element.'); },
    successMessage:'Array indexing starts at 0. [0]=first, [1]=second, [-1] doesn\'t work in JS (use .at(-1) instead).' },

  { id:20, title:'Array Push', category:'JavaScript', type:'js',
    question:'Create ["a","b"], use .push("c") to add to the end.',
    hints:['.push() adds an element to the end.','arrayName.push(newElement)','let arr = ["a","b"]; arr.push("c")'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'.push('))return F('Use .push() method.'); for(const v of Object.values(r.variables)){if(Array.isArray(v)&&v.includes('c')&&v.length>=3)return P;} return F('After push, array should contain ["a","b","c"].'); },
    successMessage:'.push() adds to end, .pop() removes from end. .unshift()/.shift() work on the beginning.' },

  { id:21, title:'Array Length', category:'JavaScript', type:'js',
    question:'Create an array with 4 elements and log its .length.',
    hints:['.length tells you how many elements.','console.log(arr.length)','let arr = [1,2,3,4]; console.log(arr.length)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'.length'))return F('Use .length property.'); return hasOutput(r,'4',true)?P:F('Array needs 4 elements. Log its .length.'); },
    successMessage:'.length works on arrays AND strings. "hello".length === 5. Useful for loops and validation.' },

  { id:22, title:'Create an Object', category:'JavaScript', type:'js',
    question:'Create object "user" with name: "Alex" and level: 1.',
    hints:['Objects use { key: value } pairs.','let obj = { key1: val1, key2: val2 }','let user = { name: "Alex", level: 1 }'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); const u=r.variables.user; return(u&&u.name==='Alex'&&u.level===1)?P:F('Create user = { name: "Alex", level: 1 }.'); },
    successMessage:'Objects store key-value pairs. They represent real-world things: users, products, configs, API responses.' },

  { id:23, title:'Object Property', category:'JavaScript', type:'js',
    question:'Create user = { name: "Alex" }, then log user.name.',
    hints:['Dot notation: object.property','console.log(obj.propertyName)','let user = { name: "Alex" }; console.log(user.name)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); return hasOutput(r,'Alex',true)?P:F('Log user.name using dot notation.'); },
    successMessage:'Dot notation (obj.key) is most common. Bracket notation (obj["key"]) works too and allows dynamic keys.' },

  { id:24, title:'Nested Objects', category:'JavaScript', type:'js',
    question:'Create an object with address: { city: "Tokyo" }. Log the city.',
    hints:['Objects can contain other objects.','Chain dots: object.outer.inner','let p = { address: { city: "Tokyo" } }; console.log(p.address.city)'],
    validate: vOut('Tokyo'),
    successMessage:'Nested objects are everywhere: API responses, configs, DOM. Optional chaining (?.) prevents errors on missing properties.' },

  { id:25, title:'Declare a Function', category:'JavaScript', type:'js',
    question:'Create function "greet" that logs "Hello!" when called. Then call it.',
    hints:['function name() { code }','Don\'t forget to call: greet()','function greet() { console.log("Hello!") }; greet()'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,/greet\s*\(\s*\)/))return F('Call the function: greet()'); return hasOutput(r,'Hello')?P:F('Function should log "Hello!" — define it and call it.'); },
    successMessage:'Functions are reusable blocks of code. Define once, call many times. They\'re the building blocks of any program.' },

  { id:26, title:'Return Value', category:'JavaScript', type:'js',
    question:'Create function "add(a, b)" that returns a + b. Log add(3, 7).',
    hints:['Use "return" to send a value back.','function add(a,b) { return a + b }','function add(a,b) { return a+b }; console.log(add(3,7))'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'return'))return F('Use "return" in the function.'); return hasOutput(r,'10')?P:F('add(3,7) should output 10.'); },
    successMessage:'Return sends a value back to the caller. Without return, functions return undefined.' },

  { id:27, title:'Arrow Function', category:'JavaScript', type:'js',
    question:'Create arrow function "double" that returns n * 2. Log double(5).',
    hints:['Arrow syntax: const name = (params) => expression','For single expressions, skip { } and return','const double = (n) => n * 2; console.log(double(5))'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'=>'))return F('Use arrow syntax =>'); return hasOutput(r,'10')?P:F('double(5) should output 10.'); },
    successMessage:'Arrow functions are concise. Single expression? Skip { } and return. const sq = n => n*n;' },

  { id:28, title:'Array Map', category:'JavaScript', type:'js',
    question:'Use .map() on [1,2,3] to double each number. Store in "doubled".',
    hints:['.map() transforms each element into a new array.','let newArr = arr.map(item => transform)','let doubled = [1,2,3].map(n => n * 2)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'.map('))return F('Use .map() method.'); const d=r.variables.doubled; return(Array.isArray(d)&&d[0]===2&&d[1]===4&&d[2]===6)?P:F(`Expected [2,4,6]. Got [${d}]`); },
    successMessage:'.map() returns a NEW array — it never modifies the original. Essential for data transformation.' },

  { id:29, title:'Array Filter', category:'JavaScript', type:'js',
    question:'Use .filter() on [1,2,3,4,5] to keep even numbers. Store in "evens".',
    hints:['.filter() keeps elements that pass a test.','Even: n % 2 === 0','let evens = [1,2,3,4,5].filter(n => n % 2 === 0)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'.filter('))return F('Use .filter() method.'); const e=r.variables.evens; return(Array.isArray(e)&&e[0]===2&&e[1]===4)?P:F(`Expected [2,4]. Got [${e}]`); },
    successMessage:'.filter() keeps elements returning true. Chain with .map() for powerful data pipelines: arr.filter(...).map(...)' },

  { id:30, title:'Ternary Operator', category:'JavaScript', type:'js',
    question:'Set score = 75. Use ternary to set status: "pass" if score > 50, else "fail".',
    hints:['Ternary: condition ? ifTrue : ifFalse','let variable = condition ? val1 : val2','let score = 75; let status = score > 50 ? "pass" : "fail"'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'?'))return F('Use the ternary operator (?).'); return hasVar(r,'status','pass')?P:F('score is 75 (>50), so status should be "pass".'); },
    successMessage:'Ternaries are one-line if-else for assignments. Keep them simple — for complex logic, use full if-else.' },

  { id:31, title:'Destructuring Object', category:'JavaScript', type:'js',
    question:'Create obj = { x: 10, y: 20 }. Use destructuring: const { x, y } = obj; Log x.',
    hints:['Destructuring extracts values: const { key } = obj','const { x, y } = obj; pulls out x and y','const obj = { x: 10, y: 20 }; const { x, y } = obj; console.log(x)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'{'))return F('Use destructuring syntax { }.'); return hasOutput(r,'10')?P:F('Log x — it should be 10.'); },
    successMessage:'Destructuring unpacks values from objects/arrays. Rename: const { x: posX } = obj. Default: const { z = 0 } = obj.' },

  { id:32, title:'Destructuring Array', category:'JavaScript', type:'js',
    question:'Create arr = [1, 2, 3]. Destructure: const [a, b, c] = arr; Log b.',
    hints:['Array destructuring uses [ ] brackets','const [first, second] = array','const arr = [1, 2, 3]; const [a, b, c] = arr; console.log(b)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'['))return F('Use array destructuring [a, b, c].'); return hasOutput(r,'2')?P:F('Log b — it should be 2.'); },
    successMessage:'Array destructuring by position. Skip with commas: const [, second] = arr. Use with swap: [a, b] = [b, a].' },

  { id:33, title:'Spread Operator', category:'JavaScript', type:'js',
    question:'Create arr1 = [1, 2]. Use spread to make arr2 = [...arr1, 3, 4].',
    hints:['... (three dots) spreads an array into another','let arr2 = [...arr1, newItems]','let arr1 = [1, 2]; let arr2 = [...arr1, 3, 4]'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'...'))return F('Use the spread operator ...'); const a=r.variables.arr2; return(Array.isArray(a)&&a.length===4&&a[0]===1&&a[3]===4)?P:F('arr2 should be [1, 2, 3, 4].'); },
    successMessage:'Spread copies arrays/objects shallowly. Merge objects: { ...obj1, ...obj2 }. Great for immutable updates.' },

  { id:34, title:'Class', category:'JavaScript', type:'js',
    question:'Create a class Animal with constructor(name) that sets this.name. Create let pet = new Animal("Rex"). Log pet.name.',
    hints:['class Name { constructor(params) { this.prop = params } }','new creates an instance','class Animal { constructor(name) { this.name = name } } let pet = new Animal("Rex"); console.log(pet.name)'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'class'))return F('Use the class keyword.'); return hasOutput(r,'Rex')?P:F('Log pet.name — should output "Rex".'); },
    successMessage:'Classes are blueprints for objects. Supports extends for inheritance, get/set for computed properties, and static methods.' },

  { id:35, title:'Closure', category:'JavaScript', type:'js',
    question:'Create a function makeCounter() that returns a function. The inner function should increment and return a counter. Log makeCounter()().',
    hints:['A closure remembers variables from its outer scope','function makeCounter() { let n = 0; return function() { n++; return n } }','function makeCounter() { let n = 0; return function() { n++; return n } } console.log(makeCounter()())'],
    validate:(c,r)=>{ if(r.errors?.length)return F(r.errors[0]); if(!codeHas(c,'return'))return F('The outer function should return the inner function.'); return hasOutput(r,'1')?P:F('First call should output 1.'); },
    successMessage:'Closures let inner functions access outer variables even after the outer function returns. Foundation of modules, callbacks, and React hooks.' },
];
