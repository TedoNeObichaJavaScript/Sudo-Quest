import { vHas } from './helpers.js';

export const JAVA_LEVELS = [
  { id:8001, title:'Hello World', category:'Java', type:'java',
    question:'Print "Hello, World!" using System.out.println.',
    hints:['System.out.println() prints with newline','Text in double quotes','System.out.println("Hello, World!");'],
    validate: vHas(['System.out.println','Hello, World']),
    successMessage:'System.out.println() adds a newline. System.out.print() does not.' },

  { id:8002, title:'Main Method', category:'Java', type:'java',
    question:'Write the main method signature for a Java program.',
    hints:['public static void main','String[] args parameter','public static void main(String[] args) { }'],
    validate: vHas(['public static void main','String']),
    successMessage:'The main method is the entry point. It must be public, static, void, with String[] args.' },

  { id:8003, title:'Integer Variable', category:'Java', type:'java',
    question:'Declare an int variable "score" set to 100.',
    hints:['int for whole numbers','int name = value;','int score = 100;'],
    validate: vHas(['int','score','100']),
    successMessage:'int is 32-bit. Java also has byte (8), short (16), long (64). All are signed.' },

  { id:8004, title:'Double Variable', category:'Java', type:'java',
    question:'Declare a double "price" set to 9.99.',
    hints:['double for decimals','double name = value;','double price = 9.99;'],
    validate: vHas(['double','price','9.99']),
    successMessage:'double is 64-bit. float is 32-bit (use f suffix: 9.99f). double is the default.' },

  { id:8005, title:'String Variable', category:'Java', type:'java',
    question:'Declare a String variable "name" with value "Alex".',
    hints:['String with capital S','String name = "value";','String name = "Alex";'],
    validate: vHas(['String','name','Alex']),
    successMessage:'String is a class, not a primitive. Strings are immutable in Java. Use StringBuilder for mutations.' },

  { id:8006, title:'Boolean Variable', category:'Java', type:'java',
    question:'Declare a boolean "isActive" set to true.',
    hints:['boolean (not bool)','lowercase true/false','boolean isActive = true;'],
    validate: vHas(['boolean','isActive','true']),
    successMessage:'Java uses "boolean" (not "bool"). Only true/false — no truthy/falsy like JS.' },

  { id:8007, title:'Scanner Input', category:'Java', type:'java',
    question:'Create a Scanner to read an integer from System.in and store it in "num".',
    hints:['Scanner is in java.util','nextInt() reads integers','Scanner sc = new Scanner(System.in); int num = sc.nextInt();'],
    validate: vHas(['Scanner','System.in','nextInt']),
    successMessage:'Scanner methods: nextInt(), nextDouble(), nextLine(), next(). Always close when done.' },

  { id:8008, title:'If Statement', category:'Java', type:'java',
    question:'Write an if that prints "Pass" when score > 50.',
    hints:['if (condition) { body }','Use System.out.println','if (score > 50) { System.out.println("Pass"); }'],
    validate: vHas(['if','score','System.out']),
    successMessage:'Parentheses around condition required. Braces define the block.' },

  { id:8009, title:'If-Else', category:'Java', type:'java',
    question:'Print "Adult" if age >= 18, "Minor" otherwise.',
    hints:['else handles alternative','Both need println','if (age >= 18) { System.out.println("Adult"); } else { System.out.println("Minor"); }'],
    validate: vHas(['if','else','System.out']),
    successMessage:'Chain with else if. Java also has the ternary: (age >= 18) ? "Adult" : "Minor".' },

  { id:8010, title:'Switch Statement', category:'Java', type:'java',
    question:'Write a switch on "day" with cases for 1 and 2, plus default.',
    hints:['switch(var) { case: ... break; }','break prevents fall-through','switch (day) { case 1: break; case 2: break; default: break; }'],
    validate: vHas(['switch','case','break','default']),
    successMessage:'Java 14+ has switch expressions with -> (no break needed). Classic switch still works.' },

  { id:8011, title:'For Loop', category:'Java', type:'java',
    question:'Write a for loop printing 1 through 5.',
    hints:['for (init; cond; update)','i++ increments','for (int i = 1; i <= 5; i++) { System.out.println(i); }'],
    validate: vHas(['for','int i','System.out']),
    successMessage:'Three parts: init, condition, update. Declare loop variable in the init section.' },

  { id:8012, title:'While Loop', category:'Java', type:'java',
    question:'Write a while loop counting 1 to 3.',
    hints:['while (condition) { body }','Increment counter','int i = 1; while (i <= 3) { System.out.println(i); i++; }'],
    validate: vHas(['while','System.out']),
    successMessage:'while checks before iteration. do-while checks after (runs at least once).' },

  { id:8013, title:'Array', category:'Java', type:'java',
    question:'Declare an int array "nums" with values 10, 20, 30.',
    hints:['int[] name = {values};','Use curly braces','int[] nums = {10, 20, 30};'],
    validate: vHas(['int[]','nums','10','20','30']),
    successMessage:'Java arrays are fixed-size. Use ArrayList for dynamic sizing.' },

  { id:8014, title:'ArrayList', category:'Java', type:'java',
    question:'Create an ArrayList of Strings called "names" and add "Alice".',
    hints:['ArrayList<Type> name = new ArrayList<>()','Use .add() to insert','ArrayList<String> names = new ArrayList<>(); names.add("Alice");'],
    validate: vHas(['ArrayList<String>','names','new ArrayList','add','Alice']),
    successMessage:'ArrayList is dynamic. Other collections: LinkedList, HashSet, HashMap, TreeMap.' },

  { id:8015, title:'Enhanced For Loop', category:'Java', type:'java',
    question:'Use an enhanced for loop to print every element in a String array "names".',
    hints:['for (Type item : array)','Iterates each element','for (String name : names) { System.out.println(name); }'],
    validate: vHas(['for','String',':','names','System.out']),
    successMessage:'Enhanced for works on arrays and any Iterable. Cannot modify the collection during iteration.' },

  { id:8016, title:'Method', category:'Java', type:'java',
    question:'Write a static method "add" taking two ints and returning their sum.',
    hints:['static for class-level methods','return sends value back','static int add(int a, int b) { return a + b; }'],
    validate: vHas(['static int add','return','a + b']),
    successMessage:'Methods define behavior. static methods belong to the class, not instances.' },

  { id:8017, title:'Method Overloading', category:'Java', type:'java',
    question:'Write two "add" methods: one for ints, one for doubles.',
    hints:['Same name, different params','Compiler picks correct one','static int add(int a, int b) { return a + b; } static double add(double a, double b) { return a + b; }'],
    validate: vHas(['int add(int','double add(double']),
    successMessage:'Overloading: same name, different parameter types/count. Return type alone is not enough.' },

  { id:8018, title:'Class', category:'Java', type:'java',
    question:'Create a class "Player" with private String "name" and public getName() method.',
    hints:['class Name { fields; methods; }','private restricts access','class Player { private String name; public String getName() { return name; } }'],
    validate: vHas(['class Player','private','String name','public','getName','return name']),
    successMessage:'Encapsulation: private fields + public getters/setters. This is core OOP in Java.' },

  { id:8019, title:'Constructor', category:'Java', type:'java',
    question:'Add a constructor to Player that takes a String name parameter.',
    hints:['Constructor has class name, no return type','this.field = param','Player(String name) { this.name = name; }'],
    validate: vHas(['Player(String','this.name','= name']),
    successMessage:'this refers to the current instance. this() calls another constructor (constructor chaining).' },

  { id:8020, title:'Inheritance', category:'Java', type:'java',
    question:'Create class "Warrior" extending Player with an int "attack" field.',
    hints:['extends for inheritance','super() calls parent constructor','class Warrior extends Player { int attack; }'],
    validate: vHas(['class Warrior extends Player','attack']),
    successMessage:'Java has single inheritance (one parent class). Use interfaces for multiple type contracts.' },

  { id:8021, title:'Override Method', category:'Java', type:'java',
    question:'Override a toString() method in Player to return the player name.',
    hints:['@Override annotation','Return type must match','@Override public String toString() { return name; }'],
    validate: vHas(['@Override','public String toString','return']),
    successMessage:'@Override is optional but recommended — compiler checks that you\'re actually overriding.' },

  { id:8022, title:'Interface', category:'Java', type:'java',
    question:'Create an interface "Attackable" with a method "attack" returning void.',
    hints:['interface Name { method(); }','Methods are abstract by default','interface Attackable { void attack(); }'],
    validate: vHas(['interface Attackable','void attack']),
    successMessage:'Interfaces define contracts. Classes "implement" them. A class can implement multiple interfaces.' },

  { id:8023, title:'Implement Interface', category:'Java', type:'java',
    question:'Make the Warrior class implement the Attackable interface.',
    hints:['implements keyword','Must define all interface methods','class Warrior extends Player implements Attackable { public void attack() { } }'],
    validate: vHas(['class Warrior','implements Attackable','void attack']),
    successMessage:'implements = promise to provide interface methods. A class can implement many interfaces.' },

  { id:8024, title:'Abstract Class', category:'Java', type:'java',
    question:'Create an abstract class "Shape" with abstract method "area" returning double.',
    hints:['abstract class cannot be instantiated','abstract method has no body','abstract class Shape { abstract double area(); }'],
    validate: vHas(['abstract class Shape','abstract double area']),
    successMessage:'Abstract classes can have both abstract and concrete methods. Interfaces (pre-Java 8) can only have abstract.' },

  { id:8025, title:'Try-Catch', category:'Java', type:'java',
    question:'Write a try-catch parsing "abc" as integer, printing "Error" on failure.',
    hints:['Integer.parseInt() converts strings','NumberFormatException on bad input','try { Integer.parseInt("abc"); } catch (NumberFormatException e) { System.out.println("Error"); }'],
    validate: vHas(['try','parseInt','catch','System.out']),
    successMessage:'Always catch specific exceptions. Use finally for cleanup. Try-with-resources auto-closes.' },

  { id:8026, title:'HashMap', category:'Java', type:'java',
    question:'Create a HashMap<String, Integer> "scores" and put "Alice" with value 95.',
    hints:['HashMap<K, V> for key-value pairs','Use .put(key, value)','HashMap<String, Integer> scores = new HashMap<>(); scores.put("Alice", 95);'],
    validate: vHas(['HashMap<String, Integer>','scores','put','Alice','95']),
    successMessage:'HashMap has O(1) average lookup. TreeMap sorts keys. LinkedHashMap preserves insertion order.' },

  { id:8027, title:'Enum', category:'Java', type:'java',
    question:'Create an enum "Color" with values RED, GREEN, BLUE.',
    hints:['enum Name { VALUES }','Constants are uppercase','enum Color { RED, GREEN, BLUE }'],
    validate: vHas(['enum Color','RED','GREEN','BLUE']),
    successMessage:'Java enums are classes — they can have fields, methods, and constructors. Very powerful.' },

  { id:8028, title:'Lambda Expression', category:'Java', type:'java',
    question:'Create a Runnable lambda that prints "Hello" and store it in a variable "task".',
    hints:['Lambdas use -> syntax','Runnable has no parameters','Runnable task = () -> System.out.println("Hello");'],
    validate: vHas(['Runnable','task','->','System.out']),
    successMessage:'Lambdas (Java 8+) implement functional interfaces. They replace verbose anonymous classes.' },

  { id:8029, title:'Stream API', category:'Java', type:'java',
    question:'Use a stream to filter even numbers from a list "nums" and collect to a new list.',
    hints:['.stream() starts a pipeline','filter() takes a predicate','nums.stream().filter(n -> n % 2 == 0).collect(Collectors.toList())'],
    validate: vHas(['.stream()','filter','collect']),
    successMessage:'Streams (Java 8+) enable functional-style operations: map, filter, reduce, collect.' },

  { id:8030, title:'Generics', category:'Java', type:'java',
    question:'Create a generic class "Box<T>" with a private field "value" of type T and a getValue() method.',
    hints:['<T> after class name','T is the type parameter','class Box<T> { private T value; public T getValue() { return value; } }'],
    validate: vHas(['class Box<T>','T value','T getValue','return value']),
    successMessage:'Generics provide compile-time type safety. Type erasure removes them at runtime.' },

  { id:8031, title:'Static Method', category:'Java', type:'java',
    question:'Write a static method "max" that returns the larger of two integers.',
    hints:['static belongs to the class','Use ternary or if','static int max(int a, int b) { return a > b ? a : b; }'],
    validate: vHas(['static int max','return']),
    successMessage:'Static methods are called on the class: MyClass.max(3, 5). They can\'t access instance fields.' },

  { id:8032, title:'String Methods', category:'Java', type:'java',
    question:'Convert a String "text" to uppercase and get its length.',
    hints:['.toUpperCase() converts','length() returns size','String upper = text.toUpperCase(); int len = text.length();'],
    validate: vHas(['.toUpperCase()','.length()']),
    successMessage:'Strings are immutable — methods return new Strings. Use StringBuilder for efficient concatenation.' },

  { id:8033, title:'File I/O', category:'Java', type:'java',
    question:'Use FileWriter to write "Hello" to "output.txt" (in a try-with-resources).',
    hints:['try (Resource r = new ...) { }','FileWriter writes text','try (FileWriter fw = new FileWriter("output.txt")) { fw.write("Hello"); }'],
    validate: vHas(['try','FileWriter','output.txt','write','Hello']),
    successMessage:'Try-with-resources auto-closes. BufferedReader/Writer add buffering for performance.' },

  { id:8034, title:'Optional', category:'Java', type:'java',
    question:'Create an Optional<String> containing "Hello" and use ifPresent to print it.',
    hints:['Optional.of() wraps a value','ifPresent takes a Consumer','Optional<String> opt = Optional.of("Hello"); opt.ifPresent(System.out::println);'],
    validate: vHas(['Optional','of(','ifPresent']),
    successMessage:'Optional (Java 8+) avoids null. Use .orElse(), .map(), .flatMap() for functional chaining.' },

  { id:8035, title:'Records', category:'Java', type:'java',
    question:'Create a record "Point" with int fields "x" and "y".',
    hints:['record is a concise class','Auto-generates constructors/getters','record Point(int x, int y) { }'],
    validate: vHas(['record Point','int x','int y']),
    successMessage:'Records (Java 16+) are immutable data carriers. They auto-generate equals, hashCode, toString.' },
];
