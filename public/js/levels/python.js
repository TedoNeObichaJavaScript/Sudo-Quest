import { vHas } from './helpers.js';

export const PYTHON_LEVELS = [
  { id:6001, title:'Hello World', category:'Python', type:'python',
    question:'Print the text "Hello, World!" to the console.',
    hints:['print() outputs text to the console','Strings go in quotes','print("Hello, World!")'],
    validate: vHas(['print','Hello, World']),
    successMessage:'print() is Python\'s built-in output function. It automatically adds a newline.' },

  { id:6002, title:'Variables', category:'Python', type:'python',
    question:'Create a variable called "name" and assign it the value "Alex".',
    hints:['Python variables need no type declaration','Use = for assignment','name = "Alex"'],
    validate: vHas(['name','=','"Alex"']),
    successMessage:'Python is dynamically typed — no need to declare types. Variables are created on assignment.' },

  { id:6003, title:'String Concatenation', category:'Python', type:'python',
    question:'Create two string variables "first" and "last", then print them combined with + operator.',
    hints:['Use + to join strings','Assign both variables first','first = "Hello"\nlast = "World"\nprint(first + last)'],
    validate: vHas(['first','last','print','+']),
    successMessage:'The + operator concatenates strings. For complex formatting, prefer f-strings: f"{first} {last}".' },

  { id:6004, title:'Integer Variable', category:'Python', type:'python',
    question:'Create an integer variable called "score" set to 100 and print it.',
    hints:['Integers are whole numbers','No quotes around numbers','score = 100\nprint(score)'],
    validate: vHas(['score','= 100','print']),
    successMessage:'Python integers have unlimited precision — no overflow. Use type() to check a value\'s type.' },

  { id:6005, title:'Float Variable', category:'Python', type:'python',
    question:'Create a float variable called "price" set to 9.99.',
    hints:['Floats are decimal numbers','Use a decimal point','price = 9.99'],
    validate: vHas(['price','9.99']),
    successMessage:'Floats represent decimal numbers. Be aware of floating-point precision issues.' },

  { id:6006, title:'Boolean Variable', category:'Python', type:'python',
    question:'Create a boolean variable called "is_active" set to True.',
    hints:['Booleans are True or False (capitalized)','No quotes around True/False','is_active = True'],
    validate: vHas(['is_active','True']),
    successMessage:'Python booleans are True and False (capitalized). They are subclasses of int: True == 1.' },

  { id:6007, title:'Type Conversion', category:'Python', type:'python',
    question:'Convert the string "42" to an integer using int() and store it in a variable called "num".',
    hints:['int() converts to integer','Pass the string to int()','num = int("42")'],
    validate: vHas(['num','int(','42']),
    successMessage:'Type casting functions: int(), float(), str(), bool(), list(), tuple().' },

  { id:6008, title:'F-Strings', category:'Python', type:'python',
    question:'Create a variable "name" set to "World", then use an f-string to print "Hello, World!".',
    hints:['f-strings start with f before the quote','Use {variable} inside','name = "World"\nprint(f"Hello, {name}!")'],
    validate: vHas(['name','f"','{name}']),
    successMessage:'f-strings (Python 3.6+) are the preferred way to format strings. Any expression works inside {}.' },

  { id:6009, title:'User Input', category:'Python', type:'python',
    question:'Use input() to ask "Enter your name: " and store the result in "name".',
    hints:['input() reads from console','Pass the prompt as a string','name = input("Enter your name: ")'],
    validate: vHas(['name','input(','Enter your name']),
    successMessage:'input() always returns a string. Use int(input()) for numbers.' },

  { id:6010, title:'If Statement', category:'Python', type:'python',
    question:'Write an if statement that checks if score is greater than 50 and prints "Pass".',
    hints:['if condition: (colon required)','Indent the body','if score > 50:\n    print("Pass")'],
    validate: vHas(['if','score','>','50','print']),
    successMessage:'Python uses indentation (4 spaces) for blocks. The colon after the condition is required.' },

  { id:6011, title:'If-Else', category:'Python', type:'python',
    question:'Write an if-else: print "Adult" when age >= 18, "Minor" otherwise.',
    hints:['else: handles the alternative','Both branches need indentation','if age >= 18:\n    print("Adult")\nelse:\n    print("Minor")'],
    validate: vHas(['if','age','else','print']),
    successMessage:'Python also has a ternary: "Adult" if age >= 18 else "Minor".' },

  { id:6012, title:'Elif', category:'Python', type:'python',
    question:'Write an if-elif-else chain: "A" if score >= 90, "B" if >= 80, "C" otherwise.',
    hints:['elif is short for "else if"','Chain as many as needed','if score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelse:\n    print("C")'],
    validate: vHas(['if','elif','else','print']),
    successMessage:'elif replaces "else if". Python 3.10+ also has match/case for pattern matching.' },

  { id:6013, title:'For Loop', category:'Python', type:'python',
    question:'Write a for loop that prints the numbers 1 through 5 using range().',
    hints:['for item in iterable:','range(1, 6) gives 1 to 5','for i in range(1, 6):\n    print(i)'],
    validate: vHas(['for','in','range','print']),
    successMessage:'for loops iterate over any iterable. Python has no C-style for(;;) loop.' },

  { id:6014, title:'While Loop', category:'Python', type:'python',
    question:'Write a while loop that prints numbers from 1 to 3.',
    hints:['while condition:','Remember to increment','i = 1\nwhile i <= 3:\n    print(i)\n    i += 1'],
    validate: vHas(['while','print']),
    successMessage:'while loops repeat as long as condition is True. Use break to exit early.' },

  { id:6015, title:'List Creation', category:'Python', type:'python',
    question:'Create a list called "fruits" containing "apple", "banana", and "cherry".',
    hints:['Lists use square brackets','Separate items with commas','fruits = ["apple", "banana", "cherry"]'],
    validate: vHas(['fruits','[','apple','banana','cherry',']']),
    successMessage:'Lists are ordered, mutable, and allow duplicates. They can hold mixed types.' },

  { id:6016, title:'List Methods', category:'Python', type:'python',
    question:'Create an empty list "items", then use .append() to add "sword".',
    hints:['.append() adds to the end','Call it on the list','items = []\nitems.append("sword")'],
    validate: vHas(['items','[]','.append(','sword']),
    successMessage:'Key list methods: append(), insert(), remove(), pop(), sort(), reverse().' },

  { id:6017, title:'List Slicing', category:'Python', type:'python',
    question:'Given a list "nums", use slicing to get the first three elements into "first_three".',
    hints:['Slicing: list[start:stop]','Stop is exclusive','first_three = nums[:3]'],
    validate: vHas(['first_three','nums[',':3]']),
    successMessage:'Slicing: list[start:stop:step]. Negative indexes count from end: nums[-1] is the last item.' },

  { id:6018, title:'Dictionary', category:'Python', type:'python',
    question:'Create a dictionary called "person" with keys "name" and "age" set to "Alice" and 30.',
    hints:['Dicts use curly braces {}','key: value pairs','person = {"name": "Alice", "age": 30}'],
    validate: vHas(['person','{','name','age','Alice','30','}']),
    successMessage:'Dicts are key-value stores with O(1) lookup. Keys must be immutable.' },

  { id:6019, title:'Dictionary Access', category:'Python', type:'python',
    question:'Access the "name" key from "person" dictionary and print it.',
    hints:['Use bracket notation','person["name"]','print(person["name"])'],
    validate: vHas(['person[','name','print']),
    successMessage:'dict["key"] raises KeyError if missing. Use .get("key", default) for safe access.' },

  { id:6020, title:'Tuple', category:'Python', type:'python',
    question:'Create a tuple called "point" with values 10 and 20.',
    hints:['Tuples use parentheses ()','point = (10, 20)','point = (10, 20)'],
    validate: vHas(['point','(','10','20']),
    successMessage:'Tuples are immutable sequences. Use them for fixed data like coordinates.' },

  { id:6021, title:'Set', category:'Python', type:'python',
    question:'Create a set called "colors" containing "red", "green", and "blue".',
    hints:['Sets use curly braces {}','No key:value, just values','colors = {"red", "green", "blue"}'],
    validate: vHas(['colors','{','red','green','blue']),
    successMessage:'Sets are unordered and unique. Great for membership testing and removing duplicates.' },

  { id:6022, title:'Function Definition', category:'Python', type:'python',
    question:'Define a function called "greet" that takes a "name" parameter and prints a greeting.',
    hints:['def keyword defines functions','def greet(name):','def greet(name):\n    print("Hello, " + name)'],
    validate: vHas(['def greet','name','print']),
    successMessage:'Functions are defined with def. They are first-class objects in Python.' },

  { id:6023, title:'Function Return', category:'Python', type:'python',
    question:'Define a function "add" that takes "a" and "b" and returns their sum.',
    hints:['return sends a value back','def add(a, b):','def add(a, b):\n    return a + b'],
    validate: vHas(['def add','return','a + b']),
    successMessage:'Without return, functions return None. Python can return multiple values as a tuple.' },

  { id:6024, title:'Default Parameters', category:'Python', type:'python',
    question:'Define a function "greet" with parameter "name" defaulting to "World".',
    hints:['Default values use = in params','def greet(name="World"):','def greet(name="World"):\n    print(f"Hello, {name}!")'],
    validate: vHas(['def greet','name','World']),
    successMessage:'Default parameters must come after non-default ones. Avoid mutable defaults like lists.' },

  { id:6025, title:'Lambda', category:'Python', type:'python',
    question:'Create a lambda called "double" that multiplies its argument by 2.',
    hints:['lambda args: expression','Single expression only','double = lambda x: x * 2'],
    validate: vHas(['double','lambda','x','* 2']),
    successMessage:'Lambdas are anonymous, single-expression functions. Great for callbacks and sorting.' },

  { id:6026, title:'List Comprehension', category:'Python', type:'python',
    question:'Create a list "squares" containing squares of numbers 1-5 using a list comprehension.',
    hints:['[expression for item in iterable]','x**2 squares a number','squares = [x**2 for x in range(1, 6)]'],
    validate: vHas(['squares','[','for','in','range']),
    successMessage:'List comprehensions are concise and Pythonic. Add conditions: [x for x in range(10) if x % 2 == 0].' },

  { id:6027, title:'Try-Except', category:'Python', type:'python',
    question:'Write a try-except that converts "abc" to int and prints "Invalid" on failure.',
    hints:['try: contains risky code','except catches the error','try:\n    int("abc")\nexcept ValueError:\n    print("Invalid")'],
    validate: vHas(['try','int(','except','print']),
    successMessage:'Always catch specific exceptions, not bare except. Use finally: for cleanup.' },

  { id:6028, title:'Class Definition', category:'Python', type:'python',
    question:'Define a class "Player" with an __init__ that accepts "name" and stores it as self.name.',
    hints:['class keyword defines classes','self refers to the instance','class Player:\n    def __init__(self, name):\n        self.name = name'],
    validate: vHas(['class Player','__init__','self','name']),
    successMessage:'__init__ is called on instantiation. self is always the first parameter.' },

  { id:6029, title:'Class Method', category:'Python', type:'python',
    question:'Add a method "greet" to a class that prints "Hello, " followed by self.name.',
    hints:['Methods are functions inside a class','self is the first parameter','def greet(self):\n    print("Hello, " + self.name)'],
    validate: vHas(['def greet(self)','print','self.name']),
    successMessage:'Instance methods always take self as first arg. Use @staticmethod for methods without self.' },

  { id:6030, title:'Inheritance', category:'Python', type:'python',
    question:'Create a class "Warrior" that inherits from Player with an "attack" attribute set to 10.',
    hints:['class Child(Parent):','Call super().__init__()','class Warrior(Player):\n    def __init__(self, name):\n        super().__init__(name)\n        self.attack = 10'],
    validate: vHas(['class Warrior(Player)','super().__init__','attack']),
    successMessage:'Python supports multiple inheritance. super() delegates to the parent class.' },

  { id:6031, title:'File Writing', category:'Python', type:'python',
    question:'Use open() with "with" to write "Hello" to "output.txt".',
    hints:['with ensures file is closed','open(filename, mode)','with open("output.txt", "w") as f:\n    f.write("Hello")'],
    validate: vHas(['with','open(','output.txt','.write(']),
    successMessage:'Always use "with" for files. Modes: "r" read, "w" write, "a" append, "rb" binary.' },

  { id:6032, title:'Import Module', category:'Python', type:'python',
    question:'Import the math module and use math.sqrt() to get the square root of 16.',
    hints:['import brings in a module','Use dot notation','import math\nresult = math.sqrt(16)'],
    validate: vHas(['import math','math.sqrt','16']),
    successMessage:'import module, from module import func. Popular: os, sys, json, datetime, random.' },

  { id:6033, title:'Decorators', category:'Python', type:'python',
    question:'Create a decorator "bold" that wraps a function\'s return in <b> tags, apply it to "greet".',
    hints:['Decorators wrap functions','@decorator above function','def bold(func):\n    def wrapper():\n        return "<b>" + func() + "</b>"\n    return wrapper\n\n@bold\ndef greet():\n    return "Hello"'],
    validate: vHas(['def bold','wrapper','@bold','def greet']),
    successMessage:'Decorators modify functions without changing their code. Use functools.wraps to preserve metadata.' },

  { id:6034, title:'Generator', category:'Python', type:'python',
    question:'Create a generator function "countdown" that yields 3, 2, 1.',
    hints:['yield makes it a generator','yield pauses and produces','def countdown():\n    yield 3\n    yield 2\n    yield 1'],
    validate: vHas(['def countdown','yield','3','2','1']),
    successMessage:'Generators produce values lazily — memory efficient for large sequences.' },

  { id:6035, title:'Args and Kwargs', category:'Python', type:'python',
    question:'Define a function "info" that accepts *args and **kwargs and prints both.',
    hints:['*args collects positional args','**kwargs collects keyword args','def info(*args, **kwargs):\n    print(args)\n    print(kwargs)'],
    validate: vHas(['def info','*args','**kwargs','print']),
    successMessage:'*args collects extra positional args as a tuple, **kwargs as a dict. Together they accept anything.' },
];
